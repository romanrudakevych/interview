var e=JSON.parse('[[1201,"Главная проблема поверхностного копирования — общее использование вложенных объектов.\\nИзменение вложенных данных в копии может неожиданно изменить исходный объект.\\nЭто часто приводит к трудноуловимым багам.\\nОсобенно опасно это в состоянии приложений и UI-фреймворках.","Поверхностное копирование выглядит безопасным, но на практике может приводить к скрытым ошибкам.\\nНеожиданные изменения данных\\nЕсли изменить вложенный объект в копии, исходный объект тоже изменится:\\n\\nСложность отладки\\nТакие эффекты опасны, потому что:\\n\\n- изменение происходит неявно\\n\\n- источник мутации трудно отследить\\n\\n- ошибка может проявиться позже\\n\\nПроблемы в React и других UI-фреймворках\\nПоверхностное копирование может:\\n\\n- ломать оптимизацию перерендеров\\n\\n- приводить к некорректному сравнению состояний\\n\\n- вызывать лишние обновления UI\\n\\nНарушение принципа иммутабельности\\nВо многих архитектурах ожидается, что данные:\\n\\n- не изменяются напрямую\\n\\n- всегда заменяются новыми объектами\\n\\nПоверхностное копирование легко нарушает это правило.\\nКраткий вывод\\nОсновной риск поверхностного копирования — скрытая мутация вложенных данных. Его нужно использовать только при полном понимании структуры объекта.","const state = {\\n  user: { name: \'Alex\' }\\n};\\n\\nconst newState = { ...state };\\nnewState.user.name = \'Bob\';\\n\\n// state.user.name тоже \'Bob\'"],[1202,"`structuredClone` — это встроенный API для глубокого копирования объектов.\\nОн корректно копирует многие типы данных, которые не поддерживает `JSON.stringify`.\\n`structuredClone` умеет работать с циклическими ссылками.\\nПри этом он сохраняет структуру данных без преобразования в строку.","`structuredClone` появился как стандартный способ безопасного глубокого копирования данных.\\nОпределение\\n`structuredClone` — это встроенная функция JavaScript, которая создаёт глубокую копию объекта, используя алгоритм структурированного клонирования.\\nЧто он умеет\\nВ отличие от JSON-подхода, `structuredClone`:\\n\\n- поддерживает циклические ссылки\\n\\n- корректно копирует `Date`\\n\\n- работает с `Map` и `Set`\\n\\n- сохраняет `ArrayBuffer` и `TypedArray`\\n\\nОграничения\\nНесмотря на преимущества, есть нюансы:\\n\\n- функции не копируются\\n\\n- DOM-узлы не поддерживаются\\n\\n- прототипы классов не сохраняются\\n\\nОтличие от `JSON.stringify`\\nКлючевые различия:\\n\\n- нет потери данных при сериализации\\n\\n- нет преобразования в строку\\n\\n- корректная работа с циклическими структурами\\n\\nКогда использовать\\n`structuredClone` подходит, если:\\n\\n- нужен безопасный deep copy\\n\\n- используются сложные структуры данных\\n\\n- важна корректность, а не совместимость со старыми браузерами\\n\\nКраткий вывод\\n`structuredClone` — современный и надёжный способ глубокого копирования, который во многих случаях превосходит JSON-подход.","const original = {\\n  date: new Date(),\\n  map: new Map([[\'a\', 1]])\\n};\\n\\nconst copy = structuredClone(original);"],[1203,"Объект можно сделать неизменяемым с помощью встроенных методов JavaScript.\\nСамый простой способ — использовать `Object.freeze`.\\nТакже можно применять иммутабельный подход, создавая новые объекты вместо изменения существующих.\\nДля глубокой неизменяемости обычно используют рекурсивную заморозку или сторонние библиотеки.","Иммутабельность — важный принцип при работе с состоянием и предсказуемыми данными.\\nОпределение\\nНеизменяемый объект — это объект, свойства которого нельзя изменить, удалить или добавить после создания.\\nИспользование `Object.freeze`\\nЭтот метод:\\n\\n- запрещает изменение существующих свойств\\n\\n- запрещает добавление новых свойств\\n\\n- запрещает удаление свойств\\n\\nОграничение `Object.freeze`\\nВажно помнить, что:\\n\\n- заморозка работает только на первом уровне\\n\\n- вложенные объекты остаются изменяемыми\\n\\nИммутабельный подход\\nАльтернативный и более гибкий вариант:\\n\\n- не менять объект напрямую\\n\\n- создавать новый объект при каждом изменении\\n\\nГлубокая неизменяемость\\nДля полной защиты можно:\\n\\n- рекурсивно применять `Object.freeze`\\n\\n- использовать библиотеки для иммутабельных структур данных\\n\\nКраткий вывод\\n`Object.freeze` защищает объект поверхностно. Для полной иммутабельности лучше использовать создание новых объектов.","const user = Object.freeze({\\n  name: \'Alex\'\\n});\\n\\nuser.name = \'Bob\'; // не сработает\\n\\nconst updatedUser = {\\n  ...user,\\n  name: \'Bob\'\\n};"],[1204,"`Object.freeze` запрещает изменение свойств объекта.\\nОн блокирует добавление, удаление и изменение свойств первого уровня.\\nОднако заморозка не является глубокой.\\nВложенные объекты остаются изменяемыми.","`Object.freeze` часто воспринимается как способ сделать объект полностью неизменяемым, но это не совсем так.\\nОпределение\\n`Object.freeze` — это метод, который делает объект нерасширяемым и незаписываемым на верхнем уровне.\\nЧто именно блокируется\\nПосле заморозки:\\n\\n- нельзя изменить значение свойства\\n\\n- нельзя добавить новое свойство\\n\\n- нельзя удалить существующее свойство\\n\\nПочему заморозка не глубокая\\nВложенные объекты:\\n\\n- не замораживаются автоматически\\n\\n- остаются изменяемыми\\n\\nПроверка заморозки\\nДля проверки можно использовать:\\n\\n- `Object.isFrozen`\\n\\n- `Object.isSealed`\\n\\nКогда этого достаточно\\n`Object.freeze` подходит, если:\\n\\n- объект плоский\\n\\n- вложенные структуры не изменяются\\n\\n- требуется базовая защита от мутаций\\n\\nКраткий вывод\\n`Object.freeze` — поверхностная защита. Для глубокой неизменяемости требуется дополнительная логика.","const config = Object.freeze({\\n  mode: \'dark\',\\n  options: { debug: true }\\n});\\n\\nconfig.options.debug = false; // допустимо"],[1205,"В Event Loop существуют несколько очередей задач.\\nОсновные из них — очередь macrotask и очередь microtask.\\nОни отличаются приоритетом выполнения и типами задач.\\nMicrotask выполняются раньше macrotask.","Асинхронная модель JavaScript построена вокруг Event Loop и очередей задач.\\nОпределение\\nEvent Loop — это механизм, который управляет выполнением синхронного кода и асинхронных задач в JavaScript.\\nОсновные очереди\\nВ процессе работы используются:\\n\\n- Macrotask queue\\n\\n- Microtask queue\\n\\nОчередь macrotask\\nВ неё попадают задачи:\\n\\n- `setTimeout`\\n\\n- `setInterval`\\n\\n- события браузера\\n\\n- I/O операции\\n\\nКаждая macrotask выполняется за один проход цикла.\\nОчередь microtask\\nЭта очередь имеет более высокий приоритет. В неё попадают:\\n\\n- `Promise.then`\\n\\n- `queueMicrotask`\\n\\n- `MutationObserver`\\n\\nВсе microtask выполняются до перехода к следующей macrotask.\\nПорядок выполнения\\nОбщий порядок выглядит так:\\n\\n- Синхронный код\\n\\n- Все microtask\\n\\n- Одна macrotask\\n\\n- Отрисовка UI\\n\\n- Следующая итерация цикла\\n\\nКраткий вывод\\nРазница между очередями заключается в приоритете. Microtask всегда выполняются раньше macrotask, что важно учитывать при работе с асинхронным кодом.",null],[1206,"Microtask и macrotask — это разные очереди задач в Event Loop.\\nMicrotask имеют более высокий приоритет и выполняются раньше.\\nMacrotask выполняются по одной за итерацию цикла.\\nНеправильное понимание их порядка часто приводит к неожиданному поведению кода.","Чтобы корректно работать с асинхронным кодом, важно понимать, какие операции попадают в какую очередь.\\nОпределение\\n\\n- Microtask — задачи с высоким приоритетом, выполняемые сразу после синхронного кода\\n\\n- Macrotask — задачи с более низким приоритетом, выполняемые по очереди между перерисовками UI\\n\\nЧто относится к microtask\\nК microtask относятся:\\n\\n- обработчики `Promise.then` / `catch` / `finally`\\n\\n- `queueMicrotask`\\n\\n- `MutationObserver`\\n\\nЧто относится к macrotask\\nК macrotask относятся:\\n\\n- `setTimeout`\\n\\n- `setInterval`\\n\\n- обработчики DOM-событий\\n\\n- сетевые и I/O операции\\n\\nПочему это важно\\nИз-за разного приоритета:\\n\\n- microtask могут выполняться раньше ожидаемого\\n\\n- порядок логов может выглядеть «сломано»\\n\\n- UI может долго не обновляться\\n\\nКраткий вывод\\nMicrotask выполняются раньше и полностью очищаются перед переходом к macrotask. Это ключевой момент при отладке асинхронного кода.","Promise.resolve().then(() => {\\n  console.log(\'microtask\');\\n});\\n\\nsetTimeout(() => {\\n  console.log(\'macrotask\');\\n}, 0);"],[1207,"Если очередь microtask постоянно пополняется, Event Loop не перейдёт к macrotask.\\nВ результате UI не будет перерисовываться.\\nПриложение может «зависнуть» без явных ошибок.\\nЭто называется starvation macrotask.","Microtask имеют абсолютный приоритет в рамках одной итерации Event Loop.\\nКак работает проблема\\nАлгоритм Event Loop устроен так:\\n\\n- выполняется синхронный код\\n\\n- выполняются все microtask\\n\\n- только потом выполняется macrotask\\n\\nЕсли каждая microtask добавляет новую microtask:\\n\\nЧто происходит в этом случае\\nВ результате:\\n\\n- очередь microtask никогда не опустеет\\n\\n- macrotask не начнут выполняться\\n\\n- отрисовка UI не произойдёт\\n\\n- браузер станет неотзывчивым\\n\\nПочему это опасно\\nТакая ситуация:\\n\\n- трудно отлаживается\\n\\n- не вызывает ошибок\\n\\n- выглядит как «заморозка» страницы\\n\\nКак избежать\\nНа практике:\\n\\n- не создают рекурсивные microtask\\n\\n- используют `setTimeout` для разрыва цикла\\n\\n- следят за логикой `Promise.then`\\n\\nКраткий вывод\\nБесконечные microtask могут полностью заблокировать приложение. Приоритет microtask нужно использовать осознанно.","function loop() {\\n  Promise.resolve().then(loop);\\n}\\nloop();"],[1208,"Отрисовка UI происходит между выполнением macrotask.\\nПеред отрисовкой браузер всегда очищает очередь microtask.\\nЕсли microtask выполняются долго, UI не обновляется.\\nПоэтому асинхронный код напрямую влияет на плавность интерфейса.","Рендеринг UI тесно связан с Event Loop, но является отдельным этапом.\\nОбщий порядок выполнения\\nТипичный порядок выглядит так:\\n\\n- Синхронный JavaScript\\n\\n- Все microtask\\n\\n- Обработка одной macrotask\\n\\n- Отрисовка UI (если требуется)\\n\\n- Следующая итерация цикла\\n\\nПочему UI ждёт microtask\\nПеред отрисовкой браузер:\\n\\n- ждёт завершения JavaScript\\n\\n- очищает очередь microtask\\n\\n- убеждается, что состояние стабильно\\n\\nЭто гарантирует консистентный рендер.\\nВлияние на производительность\\nЕсли:\\n\\n- microtask выполняются слишком долго\\n\\n- JavaScript блокирует поток\\n\\nто:\\n\\n- кадры пропускаются\\n\\n- интерфейс «дёргается»\\n\\n- появляются лаги анимаций\\n\\nПрактический вывод\\nДля плавного UI:\\n\\n- избегают тяжёлых microtask\\n\\n- используют `requestAnimationFrame` для анимаций\\n\\n- разбивают сложные вычисления на части\\n\\nКраткий вывод\\nОтрисовка UI происходит только после выполнения microtask и между macrotask. Контроль очередей напрямую влияет на отзывчивость интерфейса.",null],[1209,"В JavaScript контекст функции можно привязывать разными способами.\\nОсновные способы — `bind`, `call` и `apply`.\\nТакже контекст зависит от способа вызова функции.\\nСтрелочные функции имеют особое поведение `this`.","Контекст выполнения (`this`) в JavaScript определяется не местом объявления функции, а способом её вызова.\\nОпределение\\nКонтекст функции — это значение `this`, доступное внутри тела функции.\\nЯвная привязка контекста\\nJavaScript предоставляет методы:\\n\\n- `call` — вызывает функцию с заданным `this` и аргументами\\n\\n- `apply` — аналогичен `call`, но принимает массив аргументов\\n\\n- `bind` — возвращает новую функцию с привязанным `this`\\n\\nКонтекст через способ вызова\\nКонтекст зависит от формы вызова:\\n\\n- метод объекта → `this` указывает на объект\\n\\n- обычный вызов → `undefined` (в strict mode)\\n\\nСтрелочные функции\\nУ стрелочных функций:\\n\\n- нет собственного `this`\\n\\n- `this` берётся из внешней области видимости\\n\\nЭто удобно для колбэков и обработчиков.\\nКогда возникают ошибки\\nПроблемы с `this` часто появляются:\\n\\n- при передаче методов как колбэков\\n\\n- в обработчиках событий\\n\\n- при работе с таймерами\\n\\nКраткий вывод\\nКонтекст в JavaScript управляется способом вызова функции. Явная привязка и стрелочные функции помогают избежать ошибок с `this`.","function greet() {\\n  console.log(this.name);\\n}\\n\\nconst user = { name: \'Alex\' };\\ngreet.call(user);"],[1210,"Некоторые методы массивов изменяют сам массив, а не возвращают новый.\\nЭто называется мутацией данных.\\nМутирующие методы могут вызывать неожиданные баги, особенно при работе с состоянием.\\nВажно уметь отличать их от немутирующих методов.","При работе с массивами в JavaScript важно понимать, какие методы изменяют исходный массив.\\nОпределение\\nМутирующий метод массива — это метод, который изменяет содержимое исходного массива, а не создаёт новый.\\nОсновные мутирующие методы\\nК наиболее часто используемым относятся:\\n\\n- `push`\\n\\n- `pop`\\n\\n- `shift`\\n\\n- `unshift`\\n\\n- `splice`\\n\\n- `sort`\\n\\n- `reverse`\\n\\nПочему это может быть проблемой\\nМутации опасны, потому что:\\n\\n- исходные данные меняются неявно\\n\\n- сложнее отслеживать изменения\\n\\n- могут ломаться оптимизации и сравнения по ссылке\\n\\nНемутирующие альтернативы\\nВместо мутаций часто используют:\\n\\n- `concat`\\n\\n- `slice`\\n\\n- `map`\\n\\n- `filter`\\n\\n- `toSorted` (в современных версиях)\\n\\nПрактический вывод\\nВ бизнес-логике и UI-коде предпочтительнее немутирующие методы, чтобы код был предсказуемым и безопасным.\\nКраткий вывод\\nМутирующие методы меняют массив напрямую. Их нужно использовать осознанно и аккуратно.","const arr = [1, 2, 3];\\narr.push(4);\\n// arr теперь [1, 2, 3, 4]\\n\\nconst newArr = arr.concat(4);"],[1211,"Цепочка прототипов — это механизм, по которому JavaScript ищет свойства объекта.\\nЕсли свойство не найдено в объекте, поиск продолжается в его прототипе.\\nТакой поиск продолжается до конца цепочки.\\nЭтот механизм лежит в основе наследования в JavaScript.","JavaScript использует прототипное наследование, а не классическое, как в других языках.\\nОпределение\\nЦепочка прототипов — это последовательность объектов, по которой JavaScript ищет свойство или метод.\\nКак происходит поиск свойства\\nКогда выполняется обращение к свойству:\\n\\n- JavaScript ищет его в самом объекте\\n\\n- если не найдено — ищет в прототипе\\n\\n- процесс повторяется до конца цепочки\\n\\nПрототип и `[[Prototype]]`\\nКаждый объект имеет скрытое свойство:\\n\\n- `[[Prototype]]` — ссылка на другой объект\\n\\nЧаще всего доступ к нему получают через `Object.getPrototypeOf`.\\nГде заканчивается цепочка\\nВ стандартных объектах:\\n\\n- последним звеном является `Object.prototype`\\n\\n- дальше следует `null`\\n\\nПочему это важно\\nЦепочка прототипов:\\n\\n- позволяет переиспользовать методы\\n\\n- уменьшает дублирование кода\\n\\n- объясняет, откуда «берутся» методы у объектов\\n\\nКраткий вывод\\nЦепочка прототипов — это основа наследования в JavaScript и механизм поиска свойств.","const user = { name: \'Alex\' };\\nconsole.log(user.toString());\\n// метод найден в Object.prototype"],[1212,"При обращении к переменной, объявленной через `var`, до её объявления ошибки не будет. Переменная будет существовать, но её значение будет `undefined`. Это происходит из-за механизма поднятия объявлений. Однако инициализация значения происходит только в месте фактического объявления. Такое поведение часто приводит к трудноуловимым ошибкам.","В JavaScript объявления переменных через `var` поднимаются (hoisting) в начало функции или глобальной области видимости. Это означает, что движок JavaScript заранее знает о существовании переменной, но не о её значении.\\nКак это работает на практике\\nКогда код выполняется, JavaScript условно делает следующее:\\n\\n- Объявляет переменную (`var x`)\\n\\n- Инициализирует её значением `undefined`\\n\\n- Выполняет код сверху вниз\\n\\nПример:\\n\\nФактически это эквивалентно:\\n\\nПочему это считается проблемой\\n\\n- Код выглядит так, будто используется необъявленная переменная\\n\\n- Ошибка не возникает, что усложняет отладку\\n\\n- Поведение отличается от `let` и `const`\\n\\nВывод\\n`var` допускает использование переменной до её объявления, но это считается плохой практикой. В современном JavaScript рекомендуется использовать `let` и `const`.","console.log(a); // undefined\\nvar a = 10;\\n\\nvar a;\\nconsole.log(a);\\na = 10;"],[1213,"При обращении к переменной, объявленной через `let` или `const`, до её объявления произойдёт ошибка `ReferenceError`. Переменная существует, но недоступна до момента инициализации. Это поведение защищает код от неявных ошибок. Такое ограничение делает код более предсказуемым.","Переменные, объявленные через `let` и `const`, тоже поднимаются, но работают иначе, чем `var`. Они попадают во временную мёртвую зону (Temporal Dead Zone).\\nЧто такое Temporal Dead Zone\\nTemporal Dead Zone — это период от начала области видимости до строки с объявлением переменной, в течение которого:\\n\\n- переменная существует\\n\\n- доступ к ней запрещён\\n\\nПример:\\n\\nПочему так сделано\\n\\n- Исключается неявное использование неинициализированных переменных\\n\\n- Повышается безопасность и читаемость кода\\n\\n- Ошибки обнаруживаются раньше, а не маскируются `undefined`\\n\\nОтличие `let` и `const`\\n\\n- `let` — значение можно менять\\n\\n- `const` — повторное присваивание запрещено\\nОба подчиняются правилам TDZ.\\n\\nВывод\\n`let` и `const` намеренно запрещают доступ к переменной до объявления. Это одно из ключевых улучшений современного JavaScript по сравнению с `var`.","console.log(a); // ReferenceError\\nlet a = 10;"],[1214,"Event Loop не является частью самого JavaScript. Он реализован в среде выполнения, такой как браузер или Node.js. JavaScript определяет синтаксис и модель выполнения, но не управляет очередями задач. Именно среда выполнения отвечает за асинхронность. Поэтому поведение Event Loop может отличаться в разных окружениях.","JavaScript — это однопоточный язык, который сам по себе не умеет работать с таймерами, сетевыми запросами или событиями. Всё это предоставляет среда выполнения.\\nГде находится Event Loop\\nEvent Loop реализован:\\n\\n- в браузерах (через Web APIs)\\n\\n- в Node.js (через libuv)\\n\\nJavaScript-движок (например, V8):\\n\\n- выполняет синхронный код\\n\\n- управляет стеком вызовов\\n\\nСреда выполнения:\\n\\n- управляет очередями задач\\n\\n- решает, когда передать управление обратно движку\\n\\nКак это выглядит концептуально\\n\\n- Синхронный код выполняется в call stack\\n\\n- Асинхронные операции передаются в API среды\\n\\n- Колбэки попадают в очереди (task queue, microtask queue)\\n\\n- Event Loop решает, когда выполнить следующую задачу\\n\\nПочему это важно\\n\\n- Объясняет различия между браузером и Node.js\\n\\n- Помогает понять порядок выполнения `Promise`, `setTimeout`, `async/await`\\n\\n- Упрощает отладку асинхронного кода\\n\\nВывод\\nEvent Loop — это механизм среды выполнения, а не JavaScript-языка. Понимание этого разделения помогает глубже разобраться в асинхронности.",null],[1215,"Замыкание напрямую связано с лексическим окружением функции. Функция в JavaScript запоминает окружение, в котором была объявлена, а не в котором была вызвана. Благодаря этому она может обращаться к переменным внешней функции даже после её завершения. Лексическое окружение определяет, какие переменные доступны функции. Именно оно лежит в основе механизма замыканий.","Чтобы понять замыкания, сначала важно разобраться, что такое лексическое окружение.\\nОпределение\\nЛексическое окружение — это внутренняя структура, в которой JavaScript хранит:\\n\\n- локальные переменные\\n\\n- параметры функции\\n\\n- ссылки на внешние окружения\\n\\nКаждая функция при создании получает ссылку на лексическое окружение, в котором она была объявлена.\\nКак появляется замыкание\\nЗамыкание возникает в момент, когда:\\n\\n- Функция объявляется внутри другой функции\\n\\n- Внутренняя функция использует переменные внешней функции\\n\\n- Внутренняя функция продолжает существовать после завершения внешней\\n\\nПример:\\n\\nВ этом примере:\\n\\n- `inner` замыкает переменную `count`\\n\\n- `count` хранится в лексическом окружении `outer`\\n\\n- это окружение не удаляется, пока `inner` доступна\\n\\nПочему это работает\\nJavaScript не копирует значения переменных.\\nОн хранит ссылку на лексическое окружение, поэтому:\\n\\n- состояние сохраняется\\n\\n- данные не теряются между вызовами функции\\n\\nВажный момент\\nЗамыкание — это не отдельная сущность.\\nЭто следствие работы функций и лексических окружений.\\nВывод\\nЗамыкание — это функция плюс её лексическое окружение. Понимание этой связи позволяет уверенно работать с состоянием, колбэками и асинхронным кодом.","function outer() {\\n  let count = 0;\\n\\n  return function inner() {\\n    count++;\\n    return count;\\n  };\\n}"],[1216,"Да, функция может сохранять доступ к переменным внешней функции после её завершения. Это возможно благодаря замыканиям. Переменные не удаляются сборщиком мусора, пока на них есть ссылка. Такое поведение используется для хранения состояния. Это один из ключевых механизмов языка.","В JavaScript выполнение функции не означает автоматическое удаление всех её данных из памяти.\\nПочему переменные не удаляются\\nКогда внешняя функция завершает выполнение:\\n\\n- её стек вызова очищается\\n\\n- но лексическое окружение может остаться\\n\\nЕсли внутренняя функция:\\n\\n- возвращена\\n\\n- передана как колбэк\\n\\n- сохранена в переменной\\n\\nто она продолжает ссылаться на это окружение.\\nПример:\\n\\nЗдесь:\\n\\n- `createCounter` завершилась\\n\\n- но `value` продолжает существовать\\n\\n- потому что на него ссылается возвращённая функция\\n\\nРоль сборщика мусора\\nСборщик мусора удаляет данные только тогда, когда:\\n\\n- на них нет ссылок\\n\\n- они недостижимы из выполняемого кода\\n\\nЗамыкание удерживает ссылку на переменные, поэтому они сохраняются.\\nПрактическое значение\\n\\n- хранение состояния без глобальных переменных\\n\\n- инкапсуляция данных\\n\\n- управление доступом к значениям\\n\\nВывод\\nФункция может сохранять доступ к переменным после завершения внешней функции. Это нормальное и ожидаемое поведение JavaScript, лежащее в основе замыканий.","function createCounter() {\\n  let value = 0;\\n\\n  return function () {\\n    value++;\\n    return value;\\n  };\\n}\\n\\nconst counter = createCounter();"],[1217,"Замыкания широко используются для хранения состояния, инкапсуляции данных и работы с асинхронным кодом. Они позволяют скрывать переменные от внешнего мира. Часто применяются в обработчиках событий, таймерах и колбэках. Также замыкания лежат в основе многих паттернов проектирования. Практически любой современный JavaScript-код использует их неявно.","Замыкания — это не редкий приём, а базовый инструмент, который используется постоянно, иногда даже незаметно.\\nХранение состояния\\nЗамыкания позволяют сохранять состояние без глобальных переменных:\\n\\nИнкапсуляция данных\\nМожно скрыть детали реализации:\\n\\nЗдесь `password` недоступен снаружи.\\nАсинхронный код\\nЗамыкания активно используются в:\\n\\n- `setTimeout`\\n\\n- `Promise`\\n\\n- обработчиках событий\\n\\nФункция запоминает значения, актуальные на момент создания.\\nПаттерны и библиотеки\\nЗамыкания применяются в:\\n\\n- модулях\\n\\n- фабричных функциях\\n\\n- middleware\\n\\n- React-хуках (например, `useState`, `useCallback`)\\n\\nВозможные проблемы\\n\\n- утечки памяти при неправильном использовании\\n\\n- неожиданное сохранение устаревших данных\\n\\nВывод\\nЗамыкания используются для управления состоянием, инкапсуляции и асинхронности. Это фундаментальный механизм, без которого невозможно представить современный JavaScript.","function createToggle() {\\n  let isOpen = false;\\n\\n  return function () {\\n    isOpen = !isOpen;\\n    return isOpen;\\n  };\\n}\\n\\nfunction createUser(name) {\\n  let password = \\"secret\\";\\n\\n  return {\\n    getName() {\\n      return name;\\n    }\\n  };\\n}"],[1218,"`localStorage` и `sessionStorage` — это синхронные хранилища данных в браузере. Главное отличие — время жизни данных. `localStorage` сохраняет данные между перезагрузками и закрытием браузера. `sessionStorage` очищается при закрытии вкладки. Оба хранилища доступны только в рамках одного origin.","Браузер предоставляет несколько механизмов хранения данных, и `localStorage` с `sessionStorage` — самые простые из них.\\nОбщие характеристики\\nОба хранилища:\\n\\n- работают по принципу ключ–значение\\n\\n- хранят данные в виде строк\\n\\n- синхронные по своей природе\\n\\n- доступны через `window`\\n\\n- изолированы по origin\\n\\nОтличия по времени жизни\\n`localStorage`:\\n\\n- данные сохраняются бессрочно\\n\\n- остаются после перезагрузки страницы\\n\\n- остаются после закрытия браузера\\n\\n- подходят для долгоживущих настроек\\n\\n`sessionStorage`:\\n\\n- данные живут в рамках одной вкладки\\n\\n- очищаются при закрытии вкладки\\n\\n- не шарятся между вкладками\\n\\n- подходят для временного состояния\\n\\nПрактические примеры\\nТипичные сценарии:\\n\\n- `localStorage` — тема, язык, пользовательские настройки\\n\\n- `sessionStorage` — шаги формы, временные флаги, состояние сессии\\n\\nОграничения\\nВажно помнить:\\n\\n- объём ограничен\\n\\n- данные доступны из JavaScript\\n\\n- не подходят для чувствительной информации\\n\\nВывод\\n`localStorage` и `sessionStorage` отличаются временем жизни данных. Выбор зависит от того, нужно ли сохранять информацию между сессиями пользователя.",null],[1219,"Manual-тестирование выполняется человеком, а автоматизированное — с помощью кода. Ручное тестирование гибкое и быстрое на старте, но плохо масштабируется. Автотесты требуют времени на написание, зато экономят ресурсы в долгосрочной перспективе. В реальных проектах обычно используют комбинацию обоих подходов. Каждый тип тестирования решает свои задачи.","Тестирование — это не только про поиск багов, но и про снижение рисков при изменениях в коде.\\nManual-тестирование\\nЭто проверка приложения человеком без использования автоматических сценариев.\\nОсобенности:\\n\\n- быстро начать\\n\\n- легко проверять UI и UX\\n\\n- подходит для исследовательского тестирования\\n\\nМинусы:\\n\\n- дорого при масштабировании\\n\\n- легко пропустить регрессии\\n\\n- результат зависит от внимательности человека\\n\\nАвтоматизированное тестирование\\nЭто тесты, написанные в виде кода и выполняемые автоматически.\\nОсобенности:\\n\\n- стабильная проверка сценариев\\n\\n- хорошо ловит регрессии\\n\\n- легко интегрируется в CI/CD\\n\\nМинусы:\\n\\n- время на разработку\\n\\n- поддержка тестов при изменении UI\\n\\n- не всё удобно автоматизировать\\n\\nКак их комбинируют\\nНа практике:\\n\\n- critical flow покрывают автотестами\\n\\n- сложные UX-сценарии проверяют вручную\\n\\n- manual используют для новых фич\\n\\nВывод\\nManual и автоматизированное тестирование не конкурируют, а дополняют друг друга. Лучший результат даёт их разумное сочетание.","test(\'button click\', () => {\\n  // имитация клика и проверка результата\\n})"],[1220,"Во фронтенде используют unit, integration и end-to-end тесты. Unit-тесты проверяют отдельные функции и компоненты. Integration-тесты проверяют взаимодействие частей системы. E2E-тесты имитируют поведение пользователя в браузере. Каждый тип тестов нужен для своей зоны ответственности.","Во frontend-разработке тесты принято делить по уровню охвата и сложности.\\nUnit-тесты\\nЭто самый базовый уровень тестирования.\\nЧто проверяют:\\n\\n- функции\\n\\n- хуки\\n\\n- изолированные компоненты\\n\\nПлюсы:\\n\\n- быстрые\\n\\n- простые\\n\\n- легко поддерживать\\n\\nМинусы:\\n\\n- не ловят проблемы интеграции\\n\\nIntegration-тесты\\nПроверяют, как части приложения работают вместе.\\nПримеры:\\n\\n- компонент + API\\n\\n- форма + валидация\\n\\n- несколько связанных компонентов\\n\\nОсобенности:\\n\\n- медленнее unit-тестов\\n\\n- дают больше уверенности в логике\\n\\nEnd-to-End тесты\\nЭмулируют действия реального пользователя.\\nЧто проверяют:\\n\\n- навигацию\\n\\n- отправку форм\\n\\n- работу с реальным API\\n\\nМинусы:\\n\\n- самые медленные\\n\\n- сложны в поддержке\\n\\n- чувствительны к изменениям UI\\n\\nПрактический баланс\\nОбычно используют:\\n\\n- много unit-тестов\\n\\n- умеренное количество integration-тестов\\n\\n- минимальный набор E2E для критичных сценариев\\n\\nВывод\\nРазные типы тестов решают разные задачи. Сильная тестовая стратегия — это баланс между скоростью, надёжностью и стоимостью поддержки.",null],[1221,"Чаще всего автотесты не успевают писать из-за ограниченных сроков и приоритета фич над качеством. Бизнесу важно быстрее выпустить функциональность, а тесты не дают мгновенной ценности. Также мешают нестабильные требования и частые изменения UI. Иногда команде просто не хватает опыта или культуры тестирования. В итоге тесты откладываются «на потом».","Отсутствие автотестов почти всегда связано не с ленью, а с реальными ограничениями проекта.\\nОсновные причины\\nПеред тем как писать тесты, команда сталкивается с несколькими типичными проблемами:\\n\\n- Давление сроков\\n\\n- жёсткие дедлайны\\n\\n- фокус на delivery, а не на качество\\n\\n- тесты воспринимаются как «необязательные»\\n\\n- Частые изменения требований\\n\\n- UI и логика постоянно меняются\\n\\n- автотесты быстро ломаются\\n\\n- поддержка тестов начинает стоить дорого\\n\\n- Отсутствие зрелой тестовой стратегии\\n\\n- нет договорённостей, что именно тестировать\\n\\n- нет приоритизации critical flow\\n\\n- тесты пишутся хаотично или не пишутся вовсе\\n\\n- Недостаток экспертизы\\n\\n- разработчики не уверены, как писать хорошие тесты\\n\\n- страх увеличить сложность проекта\\n\\n- отсутствие примеров в кодовой базе\\n\\nПочему это проблема\\nБез автотестов:\\n\\n- растёт количество регрессий\\n\\n- релизы становятся рискованными\\n\\n- разработчики боятся рефакторинга\\n\\nКак обычно решают\\n\\n- начинают с тестов для критичных сценариев\\n\\n- добавляют тесты постепенно\\n\\n- закладывают время на тестирование в планирование\\n\\nВывод\\nАвтотесты чаще не пишут из-за организационных причин, а не технических. Лучший подход — писать минимум тестов для максимального снижения рисков.",null],[1222,"WebGL позволяет работать с 3D-графикой прямо в браузере, используя GPU. Это мощная, но низкоуровневая технология. На практике чаще используют библиотеки-обёртки, чтобы упростить работу. 3D применяют для визуализаций, интерактивных сцен и спецэффектов. Без реальной необходимости WebGL обычно не используют.","3D-графика во фронтенде — нишевая, но всё более востребованная область.\\nЧто такое WebGL\\nWebGL — это браузерный API для работы с 3D-графикой через GPU.\\nОсобенности:\\n\\n- низкоуровневый API\\n\\n- работа с шейдерами\\n\\n- высокая производительность\\n\\n- сложный входной порог\\n\\nКак обычно работают с 3D во фронтенде\\nНа практике редко пишут чистый WebGL.\\nЧаще используют:\\n\\n- Библиотеки-обёртки\\n\\n- упрощают работу со сценами\\n\\n- скрывают математику и шейдеры\\n\\n- ускоряют разработку\\n\\n- Интеграция с React\\n\\n- 3D-сцена живёт отдельно от React\\n\\n- React управляет состоянием и событиями\\n\\n- рендеринг идёт напрямую в canvas\\n\\nОсновные сложности\\n\\n- высокая нагрузка на GPU\\n\\n- сложность отладки\\n\\n- чувствительность к производительности устройства\\n\\nГде WebGL оправдан\\n\\n- 3D-конфигураторы\\n\\n- визуализация данных\\n\\n- интерактивные презентации\\n\\n- игровые элементы\\n\\nВывод\\nWebGL — мощный инструмент, но дорогой по сложности. Его используют только там, где обычный DOM и CSS уже не справляются.",null],[1223,"Web3 во фронтенде чаще всего сводится к работе с кошельками и смарт-контрактами через JavaScript-библиотеки. Приложение не хранит данные напрямую, а взаимодействует с блокчейном через RPC или провайдеры. Большая часть логики всё равно остаётся на клиенте и в смарт-контрактах. Важно понимать ограничения скорости и UX. Web3 — это больше про архитектуру, чем про новый синтаксис.","Во фронтенд-разработке Web3 — это прежде всего способ взаимодействия с децентрализованной инфраструктурой.\\nЧто обычно включает Web3-опыт на фронтенде\\nКак правило, фронтенд-разработчик работает со следующими задачами:\\n\\n- подключение кошелька пользователя\\n\\n- чтение данных из блокчейна\\n\\n- отправка транзакций\\n\\n- отображение статуса операций\\n\\nКак устроено взаимодействие\\nФронтенд не общается с блокчейном напрямую, а использует:\\n\\n- Провайдер\\n\\n- точка входа в блокчейн-сеть\\n\\n- может быть браузерной или удалённой\\n\\n- JavaScript-библиотеки\\n\\n- абстрагируют RPC-вызовы\\n\\n- упрощают работу с контрактами\\n\\n- скрывают детали форматов данных\\n\\nОсновные сложности\\n\\n- высокая задержка подтверждения транзакций\\n\\n- ошибки из-за газа и комиссий\\n\\n- сложный UX для пользователей\\n\\n- невозможность мгновенного отката операций\\n\\nГде Web3 оправдан\\n\\n- DeFi-приложения\\n\\n- NFT-платформы\\n\\n- DAO-интерфейсы\\n\\n- проекты с требованием децентрализации\\n\\nВывод\\nWeb3 во фронтенде — это не про сложный код, а про понимание ограничений блокчейна и умение строить понятный UX поверх медленной и неизменяемой системы.","// пример запроса данных из смарт-контракта\\ncontract.methods.balanceOf(address).call()"],[1224,"NFT — это уникальные токены в блокчейне, которые представляют право владения цифровым объектом. В веб-приложениях NFT используют для подтверждения собственности и доступа к функциям. Фронтенд обычно отображает метаданные токена и взаимодействует с кошельком пользователя. Сам контент часто хранится вне блокчейна. NFT — это больше про идентификацию, чем про сами файлы.","NFT часто воспринимают как картинки, но технически это лишь малая часть концепции.\\nЧто такое NFT\\nNFT (Non-Fungible Token) — это:\\n\\n- уникальный токен\\n\\n- с индивидуальным идентификатором\\n\\n- записанный в блокчейне\\n\\nВ отличие от обычных токенов, NFT нельзя заменить другим таким же.\\nЧто хранится в NFT\\nОбычно в NFT содержится:\\n\\n- ссылка на метаданные\\n\\n- информация о владельце\\n\\n- уникальный ID токена\\n\\nСам файл (изображение, видео) чаще всего хранится:\\n\\n- вне блокчейна\\n\\n- в распределённом хранилище\\n\\nКак NFT используются во фронтенде\\nФронтенд-приложения обычно:\\n\\n- подключают кошелёк пользователя\\n\\n- получают список NFT\\n\\n- отображают метаданные\\n\\n- проверяют права доступа\\n\\nТиповые сценарии\\n\\n- цифровое искусство\\n\\n- доступ к закрытым разделам сайта\\n\\n- игровые предметы\\n\\n- билеты и пропуска\\n\\nВывод\\nNFT — это инструмент для подтверждения уникального владения. Во фронтенде они используются как источник прав и состояния, а не как хранилище контента.","// проверка владения токеном\\nconst ownsToken = tokens.includes(tokenId)"],[1225,"Express и Nest — популярные Node.js-фреймворки для создания серверных приложений. Express минималистичный и даёт много свободы, но требует больше ручной организации. Nest предлагает более строгую архитектуру и удобен для масштабных проектов. Фронтенд-разработчику важно понимать их на уровне API и контрактов. Глубокое знание не всегда обязательно, но понимание принципов — полезно.","Фронтенд-разработчик часто взаимодействует с backend-кодом, поэтому базовое понимание серверных фреймворков сильно упрощает работу.\\nExpress\\nExpress — минималистичный HTTP-фреймворк.\\nОсновные особенности:\\n\\n- простой и гибкий\\n\\n- минимальное количество абстракций\\n\\n- быстрый старт\\n\\nТипичный сценарий:\\n\\n- небольшие сервисы\\n\\n- прототипы\\n\\n- простые API\\n\\nМинусы:\\n\\n- архитектура полностью на ответственности разработчика\\n\\n- сложнее поддерживать большие проекты\\n\\n- нет строгих правил структуры\\n\\nNest\\nNest — фреймворк с чёткой архитектурой.\\nКлючевые идеи:\\n\\n- модульность\\n\\n- dependency injection\\n\\n- явное разделение слоёв\\n\\nПлюсы:\\n\\n- хорошо подходит для больших команд\\n\\n- проще масштабировать\\n\\n- код более предсказуемый\\n\\nМинусы:\\n\\n- выше порог входа\\n\\n- больше boilerplate\\n\\nЗачем это фронтендеру\\nПонимание backend-фреймворков помогает:\\n\\n- договариваться о формате API\\n\\n- понимать ограничения сервера\\n\\n- быстрее дебажить проблемы на границе фронта и бэка\\n\\nВывод\\nExpress и Nest решают разные задачи. Фронтенд-разработчику достаточно понимать их философию и влияние на API, не углубляясь в детали реализации.","app.get(\'/users\', (req, res) => {\\n  res.json(users)\\n})"],[1226,"Обычно выгодно сначала делать `filter`, чтобы уменьшить объем данных для следующих шагов. `sort` часто выполняют ближе к концу, потому что сортировка дороже по времени и при этом мутирует массив, если не скопировать его. `map` логично делать после фильтрации, чтобы не вычислять лишнее. `reduce` чаще ставят последним, когда нужно собрать итог (число, объект, Map и т.д.).","Порядок операций выбирают так, чтобы делать дорогие действия на меньшем объеме данных и избегать лишних вычислений.\\nОпределение\\n\\n- `filter` — отбирает элементы по условию и возвращает новый массив.\\n\\n- `map` — преобразует каждый элемент и возвращает новый массив.\\n\\n- `sort` — сортирует массив на месте и возвращает ссылку на тот же массив.\\n\\n- `reduce` — сворачивает массив в одно значение (например, число или объект).\\n\\nПрактическое правило по умолчанию\\n\\n- `filter` — уменьшить массив как можно раньше.\\n\\n- `map` — преобразовать только то, что осталось после фильтра.\\n\\n- `sort` — сортировать ближе к концу, уже готовые данные (и обычно после `map`, если сортировка зависит от вычисленного поля).\\n\\n- `reduce` — финальная агрегация/сбор результата.\\n\\nПочему так\\n\\n- `filter` и `map` — это линейные проходы по массиву.\\n\\n- `sort` обычно дороже (сравнения и перестановки), поэтому лучше сортировать меньший массив.\\n\\n- `sort` мутирует массив — чтобы не сломать исходные данные, часто делают копию.\\n\\nПример\\n\\nЕсли `toSorted` недоступен, делайте копию:\\n\\nВажные исключения\\n\\n- Если `sort` должен учитывать поля исходных элементов, а `map` эти поля убирает — сортируйте раньше или сохраняйте поля.\\n\\n- Если `reduce` заменяет `map` + `filter` (в один проход) — это может быть быстрее, но хуже читаемость.\\n\\nВывод\\nЧаще всего используйте порядок `filter → map → sort → reduce`, а сортировку делайте безопасно (через копию или `toSorted`), чтобы не мутировать исходный массив.","const users = [\\n  { name: \\"Ann\\", age: 21 },\\n  { name: \\"Bob\\", age: 17 },\\n  { name: \\"Cody\\", age: 30 },\\n];\\n\\nconst result = users\\n  .filter(u => u.age >= 18)\\n  .map(u => ({ ...u, label: `${u.name} (${u.age})` }))\\n  .toSorted((a, b) => a.age - b.age) // если нет toSorted — см. ниже\\n  .reduce((acc, u) => {\\n    acc.push(u.label);\\n    return acc;\\n  }, []);\\n\\nconsole.log(result);\\n\\nconst sorted = [...arr].sort((a, b) => a - b);"],[1227,"`split` — это метод строки: он превращает строку в массив по разделителю. `reverse` — метод массива: он переворачивает массив на месте. `join` — метод массива: он склеивает элементы массива в строку с разделителем. Обычно эти методы используют вместе, например, чтобы развернуть строку: `str.split(\'\').reverse().join(\'\')`.","Эти методы часто путают, потому что их комбинируют в цепочках, но они относятся к разным типам.\\nОпределение\\n\\n- `split` (String.prototype.split)\\nПревращает строку в массив подстрок.\\n\\n- `reverse` (Array.prototype.reverse)\\nПереворачивает массив и мутирует исходный массив.\\n\\n- `join` (Array.prototype.join)\\nСклеивает элементы массива в строку.\\n\\nНа каких типах работают\\n\\n- `split` работает на строке:\\n\\n- возвращает массив строк.\\n\\n- `reverse` работает на массиве:\\n\\n- возвращает тот же массив, но уже перевернутый.\\n\\n- `join` работает на массиве:\\n\\n- возвращает строку.\\n\\nПримеры\\nРазбить CSV-строку:\\n\\nПеревернуть массив:\\n\\nСклеить массив в строку:\\n\\nЧастый кейс: развернуть строку\\n\\nПодводные камни\\n\\n- `reverse()` мутирует массив — если массив нужен дальше в исходном порядке, делайте копию:\\n\\n- `split(\\"\\")` ломает некоторые символы (например, эмодзи) на части, потому что они могут состоять из нескольких кодовых единиц.\\n\\nВывод\\n`split` — строка → массив, `reverse` — переворот массива (с мутацией), `join` — массив → строка. Вместе они дают удобные преобразования, но важно помнить про мутацию `reverse`.","const s = \\"a,b,c\\";\\nconst parts = s.split(\\",\\");\\nconsole.log(parts); // [\\"a\\", \\"b\\", \\"c\\"]\\n\\nconst arr = [1, 2, 3];\\narr.reverse();\\nconsole.log(arr); // [3, 2, 1] (исходный массив изменился)\\n\\nconst arr = [\\"a\\", \\"b\\", \\"c\\"];\\nconst s = arr.join(\\"-\\");\\nconsole.log(s); // \\"a-b-c\\"\\n\\nconst s = \\"hello\\";\\nconst reversed = s.split(\\"\\").reverse().join(\\"\\");\\nconsole.log(reversed); // \\"olleh\\"\\n\\nconst reversed = [...arr].reverse();"],[1228,"Проще всего преобразовать строку в массив, удалить дубли через `Set` и собрать обратно. Если нужно удалить дубли символов — используйте `new Set(str)` и `join`. Если нужно удалить дубли слов — сначала разбейте строку по пробелам (`split`), примените `Set`, затем `join`. Важно уточнить, что именно считается “дублем”: символы, слова или подстроки.","Способ зависит от того, что именно дублируется и как нужно сохранить порядок.\\nОпределение\\n\\n- Дедупликация — удаление повторяющихся элементов, оставляя только уникальные.\\n\\nДубли символов (оставляем первое вхождение, сохраняем порядок)\\n\\nЧто происходит\\n\\n- `new Set(s)` перебирает строку как последовательность символов и хранит уникальные.\\n\\n- `[...]` превращает `Set` обратно в массив.\\n\\n- `join(\\"\\")` собирает строку.\\n\\nДубли слов (разделитель — пробел)\\n\\nВариант: нормализация (если важно убрать лишние пробелы)\\n\\nЕсли важно сохранить регистр или наоборот игнорировать его\\nИгнорировать регистр, но сохранить оригинальный вид первого встретившегося слова:\\n\\nВывод\\nДля удаления дублей в строке чаще всего используют `Set`:\\n\\n- для символов: `[...] + Set + join(\\"\\")`\\n\\n- для слов/токенов: `split(...) + Set + join(...)`\\nКлючевое — правильно выбрать “единицу уникальности” (символ, слово, токен) и нужный разделитель.","const s = \\"abacaba\\";\\nconst unique = [...new Set(s)].join(\\"\\");\\nconsole.log(unique); // \\"abc\\"\\n\\nconst s = \\"cat dog cat bird dog\\";\\nconst unique = [...new Set(s.split(\\" \\"))].join(\\" \\");\\nconsole.log(unique); // \\"cat dog bird\\"\\n\\nconst s = \\"cat  dog   cat\\";\\nconst words = s.trim().split(/\\"\\"s+/); // несколько пробелов → один разделитель\\nconst unique = [...new Set(words)].join(\\" \\");\\nconsole.log(unique); // \\"cat dog\\"\\n\\nconst s = \\"Cat cat DOG dog\\";\\nconst words = s.split(\\" \\");\\n\\nconst seen = new Set();\\nconst unique = [];\\n\\nfor (const w of words) {\\n  const key = w.toLowerCase();\\n  if (!seen.has(key)) {\\n    seen.add(key);\\n    unique.push(w);\\n  }\\n}\\n\\nconsole.log(unique.join(\\" \\")); // \\"Cat DOG\\""],[1229,"Да, `Map` может использовать любое значение в качестве ключа: примитивы, объекты, массивы, функции. В отличие от обычного объекта, ключи в `Map` не приводятся к строке. Для объектов и функций сравнение ключей происходит по ссылке, а не по содержимому. Это делает `Map` удобным для хранения данных, привязанных к конкретным объектам.","`Map` — это коллекция пар ключ–значение, где ключом может быть любой тип данных без неявных преобразований.\\nОпределение\\n`Map` — структура данных, которая хранит пары ключ–значение и сравнивает ключи по принципу SameValueZero (почти как `===`, но с `NaN`).\\nКакие типы могут быть ключами\\n\\n- Примитивы:\\n\\n- `string`\\n\\n- `number`\\n\\n- `boolean`\\n\\n- `null`\\n\\n- `undefined`\\n\\n- `symbol`\\n\\n- Ссылочные типы:\\n\\n- `object`\\n\\n- `array`\\n\\n- `function`\\n\\nПример с примитивами\\n\\nКлючи-объекты и сравнение по ссылке\\n\\nПочему так\\n\\n- Объекты сравниваются по ссылке, а не по структуре.\\n\\n- Даже одинаковые по содержимому объекты — разные ключи, если это разные ссылки.\\n\\nОтличие от обычного объекта\\n\\n- В объекте ключи всегда приводятся к строке.\\n\\n- В `Map` ключ сохраняет исходный тип.\\n\\n- `Map` безопасен для ключей-объектов и не конфликтует с прототипом.\\n\\nПример проблемы с объектом\\n\\nВывод\\n`Map` может использовать любые типы данных как ключи и особенно полезен, когда ключами должны быть объекты или функции и важно сохранить их идентичность по ссылке.","const map = new Map();\\n\\nmap.set(\\"id\\", 1);\\nmap.set(42, \\"answer\\");\\n\\nconsole.log(map.get(\\"id\\")); // 1\\nconsole.log(map.get(42));   // \\"answer\\"\\n\\nconst map = new Map();\\n\\nconst user = { name: \\"Ann\\" };\\nmap.set(user, \\"data\\");\\n\\nconsole.log(map.get(user)); // \\"data\\"\\nconsole.log(map.get({ name: \\"Ann\\" })); // undefined\\n\\nconst obj = {};\\nconst key = { a: 1 };\\n\\nobj[key] = \\"value\\";\\nconsole.log(obj); // { \\"[object Object]\\": \\"value\\" }"],[1230,"`WeakMap` отличается тем, что его ключи могут быть только объектами, и они хранятся как слабые ссылки. Если на объект-ключ больше нет сильных ссылок, он удаляется сборщиком мусора вместе со значением. В `WeakMap` нельзя итерироваться по элементам и узнать размер. `Map` таких ограничений не имеет и удерживает данные в памяти.","Основное различие между `Map` и `WeakMap` — это влияние на управление памятью.\\nОпределение\\n`WeakMap` — коллекция пар ключ–значение, где ключи являются объектами и не препятствуют их сборке мусора.\\nОграничения WeakMap\\n\\n- Ключи — только объекты.\\n\\n- Нет методов:\\n\\n- `keys()`\\n\\n- `values()`\\n\\n- `entries()`\\n\\n- `size`\\n\\n- Нельзя итерироваться.\\n\\nПример\\n\\nПочему нельзя итерироваться\\n\\n- Объекты могут быть удалены в любой момент.\\n\\n- JS-движок не обязан сообщать, когда именно это произойдет.\\n\\n- Итерация нарушила бы абстракцию сборки мусора.\\n\\nКогда использовать WeakMap\\n\\n- Хранение метаданных, связанных с объектами.\\n\\n- Кэш, который не должен приводить к утечкам памяти.\\n\\n- Приватные данные, привязанные к экземплярам.\\n\\nТипичный кейс\\n\\nКогда нужен Map\\n\\n- Нужна итерация по данным.\\n\\n- Ключи — примитивы.\\n\\n- Важно явно управлять временем жизни данных.\\n\\nВывод\\n`WeakMap` используют для безопасной работы с памятью и объектами, а `Map` — для универсальных коллекций с полным доступом и итерацией.","const weakMap = new WeakMap();\\n\\nlet user = { name: \\"Ann\\" };\\nweakMap.set(user, \\"metadata\\");\\n\\nuser = null;\\n// объект может быть удалён сборщиком мусора\\n\\nconst privateData = new WeakMap();\\n\\nfunction User(name) {\\n  privateData.set(this, { name });\\n}\\n\\nUser.prototype.getName = function () {\\n  return privateData.get(this).name;\\n};"],[1231,"Счётчик через замыкание реализуется функцией, которая хранит состояние во внешней переменной и возвращает внутреннюю функцию. Эта внутренняя функция имеет доступ к переменной даже после завершения внешней функции. Таким образом состояние становится приватным и недоступным напрямую извне. Каждый вызов фабричной функции создаёт независимый счётчик.","Замыкание позволяет функции “помнить” переменные из той области видимости, где она была создана.\\nОпределение\\nЗамыкание (closure) — это функция вместе с лексическим окружением, в котором она была объявлена.\\nБазовая реализация счётчика\\n\\nЧто здесь происходит\\n\\n- `count` — локальная переменная `createCounter`.\\n\\n- Возвращаемая функция имеет доступ к `count`.\\n\\n- `count` не удаляется после завершения `createCounter`.\\n\\nНесколько независимых счётчиков\\n\\nРасширенный вариант с методами\\n\\nПочему это полезно\\n\\n- Состояние инкапсулировано.\\n\\n- Нет глобальных переменных.\\n\\n- Поведение предсказуемо и тестируемо.\\n\\nВывод\\nСчётчик через замыкание — классический пример использования лексической области видимости для хранения приватного состояния и создания независимых экземпляров логики.","function createCounter() {\\n  let count = 0;\\n\\n  return function () {\\n    count += 1;\\n    return count;\\n  };\\n}\\n\\nconst counter = createCounter();\\n\\ncounter(); // 1\\ncounter(); // 2\\ncounter(); // 3\\n\\nconst c1 = createCounter();\\nconst c2 = createCounter();\\n\\nc1(); // 1\\nc1(); // 2\\nc2(); // 1\\n\\nfunction createCounter() {\\n  let count = 0;\\n\\n  return {\\n    inc() {\\n      count += 1;\\n      return count;\\n    },\\n    reset() {\\n      count = 0;\\n    },\\n    get() {\\n      return count;\\n    }\\n  };\\n}"],[1232,"Оператор `??` возвращает правый операнд, только если левый равен `null` или `undefined`. Во всех остальных случаях возвращается левое значение. В отличие от `||`, он не считает `0`, `false` или пустую строку ошибочными значениями. Это делает `??` безопасным для задания значений по умолчанию.","`??` решает типичную проблему оператора `||`, который подменяет значение по умолчанию при любом falsy-значении.\\nОпределение\\nNullish coalescing (`??`) — оператор, возвращающий правый операнд, если левый равен `null` или `undefined`.\\nБазовый пример\\n\\nДля сравнения с `||`:\\n\\nКогда `??` особенно полезен\\n\\n- Значение `0` является валидным.\\n\\n- Пустая строка `\\"\\"` имеет смысл.\\n\\n- `false` — корректное логическое состояние.\\n\\nПример с настройками\\n\\nОграничения использования\\n\\n- Нельзя смешивать `??` с `||` или `&&` без скобок.\\n\\nВывод\\n`??` стоит использовать для задания значений по умолчанию, когда важно отличать `null/undefined` от других допустимых значений.","const value = 0;\\nconst result = value ?? 10;\\n\\nconsole.log(result); // 0\\n\\nconst result = value || 10;\\nconsole.log(result); // 10\\n\\nfunction init(options) {\\n  const timeout = options.timeout ?? 3000;\\n  const debug = options.debug ?? false;\\n\\n  // ...\\n}\\n\\n// Ошибка\\na ?? b || c;\\n\\n// Правильно\\na ?? (b || c);"],[1233,"`spread` используется для раскрытия элементов массива или объекта. `rest` используется для сбора элементов в массив или объект. Визуально это один и тот же `...`, но смысл зависит от места использования. `spread` чаще применяют при копировании и объединении данных, `rest` — в параметрах функций и деструктуризации","Оператор `...` имеет два разных семантических значения.\\nОпределение\\n\\n- Spread — раскрывает элементы структуры.\\n\\n- Rest — собирает несколько элементов в одну переменную.\\n\\nSpread оператор\\nИспользуется при:\\n\\n- Копировании массивов и объектов.\\n\\n- Объединении данных.\\n\\n- Передаче аргументов в функцию.\\n\\nПередача аргументов:\\n\\nRest оператор\\nИспользуется при:\\n\\n- Параметрах функции.\\n\\n- Деструктуризации.\\n\\nВ деструктуризации:\\n\\nВажный нюанс\\n\\n- Spread создаёт поверхностную копию.\\n\\n- Вложенные объекты копируются по ссылке.\\n\\nВывод\\nSpread — это «развернуть», rest — это «собрать». Один синтаксис, но разные задачи и контексты применения.","const arr = [1, 2, 3];\\nconst copy = [...arr];\\n\\nconst obj = { a: 1 };\\nconst extended = { ...obj, b: 2 };\\n\\nconst nums = [1, 2, 3];\\nMath.max(...nums);\\n\\nfunction sum(...numbers) {\\n  return numbers.reduce((a, b) => a + b, 0);\\n}\\n\\nconst [first, ...rest] = [1, 2, 3];"],[1234,"Template literals — это строки в обратных кавычках (```), которые поддерживают интерполяцию выражений и многострочный текст. Они позволяют вставлять переменные и выражения прямо в строку через `${}`. Это делает код короче и читабельнее по сравнению с конкатенацией. Также они удобны для HTML-шаблонов и логирования.","Template literals упрощают работу со строками и форматированием текста.\\nОпределение\\nTemplate literals — строковый синтаксис JavaScript, использующий обратные кавычки и поддерживающий интерполяцию выражений.\\nИнтерполяция значений\\n\\nВычисления внутри строки\\n\\nМногострочные строки\\n\\nТипичные сценарии использования\\n\\n- Формирование сообщений и логов.\\n\\n- Генерация HTML или CSS.\\n\\n- Работа с локализацией.\\n\\n- Повышение читаемости кода.\\n\\nTagged template literals (кратко)\\nTemplate literals могут передаваться в функцию для кастомной обработки строки, например для i18n или экранирования, но это используется реже.\\nВывод\\nTemplate literals делают строки гибкими, читаемыми и удобными, особенно при динамическом формировании текста и шаблонов.","const name = \\"Alex\\";\\nconst age = 30;\\n\\nconst text = `User ${name} is ${age} years old`;\\n\\nconst a = 2;\\nconst b = 3;\\n\\nconst result = `Sum: ${a + b}`;\\n\\nconst html = `\\n  <div>\\n    <span>Hello</span>\\n  </div>\\n`;"],[1235,"Microtasks — это задачи с более высоким приоритетом, которые выполняются сразу после завершения текущего синхронного кода. К ним относятся `Promise.then`, `catch`, `finally`, `queueMicrotask`, `MutationObserver`. Macrotasks — это задачи общего планирования, такие как `setTimeout`, `setInterval`, `setImmediate`, обработчики событий и I/O. Event loop всегда сначала выполняет все microtasks, и только потом берёт следующую macrotask.","Чтобы понять разницу между microtasks и macrotasks, важно разобраться, как работает event loop.\\nОпределение\\nEvent loop — механизм JavaScript, который управляет выполнением синхронного и асинхронного кода, используя очередь задач.\\nMacrotasks (Task queue)\\nК macrotasks относятся:\\n\\n- `setTimeout`\\n\\n- `setInterval`\\n\\n- `setImmediate` (Node.js)\\n\\n- Обработчики DOM-событий\\n\\n- I/O операции\\n\\nMicrotasks (Microtask queue)\\nК microtasks относятся:\\n\\n- `Promise.then / catch / finally`\\n\\n- `queueMicrotask`\\n\\n- `MutationObserver`\\n\\nПорядок выполнения\\n\\n- Выполняется весь синхронный код.\\n\\n- Выполняются все microtasks.\\n\\n- Выполняется одна macrotask.\\n\\n- Снова выполняются все microtasks.\\n\\n- Цикл повторяется.\\n\\nПример\\n\\nРезультат:\\n\\nПочему microtasks важны\\n\\n- Они гарантируют предсказуемое завершение асинхронных цепочек.\\n\\n- Позволяют `Promise` выполняться раньше таймеров.\\n\\n- Используются внутри `async/await`.\\n\\nВывод\\nMicrotasks имеют более высокий приоритет и всегда выполняются перед macrotasks, что критично для понимания порядка выполнения асинхронного JavaScript-кода.","setTimeout(() => {\\n  console.log(  imeout\\");\\n}, 0);\\n\\nPromise.resolve().then(() => {\\n  console.log(\\"promise\\");\\n});\\n\\nconsole.log(\\"start\\");\\n\\nsetTimeout(() => console.log(  imeout\\"));\\n\\nPromise.resolve().then(() => console.log(\\"promise\\"));\\n\\nconsole.log(\\"end\\");\\n\\nstart\\nend\\npromise\\ntimeout"],[1236,"`Promise.then` всегда выполняется раньше, чем `setTimeout`, даже если таймер имеет задержку `0`. Это происходит потому, что `then` попадает в очередь microtasks, а `setTimeout` — в очередь macrotasks. После завершения синхронного кода event loop сначала очищает очередь microtasks, и только потом берёт следующую macrotask.","Этот вопрос — частный случай работы event loop, но его часто задают на собеседованиях.\\nОпределение\\n`Promise.then` — microtask\\n`setTimeout` — macrotask\\nПример\\n\\nРезультат\\n\\nБолее сложный пример\\n\\nПорядок:\\n\\n- Синхронный код завершился.\\n\\n- Выполнился `promise`.\\n\\n- Выполнился `timeout`.\\n\\n- Выполнился `promise in timeout`.\\n\\nВывод\\n`Promise.then` имеет приоритет над `setTimeout`, потому что microtasks выполняются раньше macrotasks.","setTimeout(() => {\\n  console.log(  imeout\\");\\n}, 0);\\n\\nPromise.resolve().then(() => {\\n  console.log(\\"promise\\");\\n});\\n\\npromise\\ntimeout\\n\\nsetTimeout(() => {\\n  console.log(  imeout\\");\\n  Promise.resolve().then(() => console.log(\\"promise in timeout\\"));\\n}, 0);\\n\\nPromise.resolve().then(() => {\\n  console.log(\\"promise\\");\\n});"],[1237,"Любая `async`-функция всегда возвращает `Promise`. Если функция возвращает обычное значение, оно автоматически оборачивается в `Promise.resolve`. Если внутри происходит `throw`, возвращается `Promise.reject`. `await` не меняет тип возвращаемого значения функции.","`async/await` — это синтаксический сахар над `Promise`.\\nОпределение\\nAsync-функция — функция, которая всегда возвращает `Promise`, независимо от того, что возвращается внутри.\\nПримеры возврата значения\\n\\nЭквивалентно:\\n\\nПример с ошибкой\\n\\nИспользование await\\n\\nВажно понимать\\n\\n- `await` приостанавливает выполнение функции, но не блокирует основной поток.\\n\\n- Внешний код всегда работает с `Promise`.\\n\\nВывод\\n`async`-функция — это всегда `Promise`, а `await` лишь упрощает работу с асинхронным кодом, не меняя модель выполнения.","async function getNumber() {\\n  return 5;\\n}\\n\\ngetNumber().then(console.log); // 5\\n\\nfunction getNumber() {\\n  return Promise.resolve(5);\\n}\\n\\nasync function fail() {\\n  throw new Error(\\"Oops\\");\\n}\\n\\nfail().catch(console.error);\\n\\nasync function fetchData() {\\n  const data = await Promise.resolve(\\"data\\");\\n  return data;\\n}"],[1238,"`async/await` и `.then()` работают поверх `Promise` и дают одинаковые возможности. Разница в том, что `async/await` позволяет писать асинхронный код в синхронном стиле, улучшая читаемость. `.then()` использует цепочки колбэков и чаще приводит к вложенности. С точки зрения выполнения и производительности они эквивалентны.","Оба подхода решают одну и ту же задачу, но по-разному организуют код.\\nОпределение\\n\\n- `.then()` — метод `Promise`, принимающий колбэк, который выполнится после резолва.\\n\\n- `async/await` — синтаксический сахар над `Promise`, позволяющий писать асинхронный код линейно.\\n\\nПример с `.then()`\\n\\nТот же пример с async/await\\n\\nОсновные отличия\\n\\n- Читаемость\\n\\n- `async/await` читается сверху вниз.\\n\\n- `.then()` сложнее при длинных цепочках.\\n\\n- Обработка ошибок\\n\\n- `async/await` использует `try/catch`.\\n\\n- `.then()` — `.catch()`.\\n\\n- Отладка\\n\\n- Стек вызовов проще читать с `async/await`.\\n\\n- Гибкость\\n\\n- `.then()` удобно для коротких цепочек.\\n\\n- `async/await` — для сложной логики.\\n\\nВывод\\nВыбор между `async/await` и `.then()` — вопрос читаемости и стиля, а не возможностей или производительности.","fetchData()\\n  .then(data => process(data))\\n  .then(result => save(result))\\n  .catch(error => handleError(error));\\n\\nasync function run() {\\n  try {\\n    const data = await fetchData();\\n    const result = await process(data);\\n    await save(result);\\n  } catch (error) {\\n    handleError(error);\\n  }\\n}"],[1239,"`setInterval` запускает обработчик по расписанию, даже если предыдущий запрос ещё не закончился, из-за чего появляются параллельные запросы и лишняя нагрузка. Интервалы также “плывут” (drift): если обработчик выполняется долго, фактические моменты запуска смещаются. `setTimeout` удобнее для polling, потому что позволяет запускать следующий запрос после завершения предыдущего и гибко менять задержку (например, при ошибках увеличить).","Polling — это повторяющиеся запросы “проверить состояние”. Для него важно контролировать конкуренцию и частоту.\\nОпределение\\nОпределение: Polling — периодическая проверка сервера клиентом (например, “появились ли новые данные?”).\\nПроблемы setInterval в polling\\n\\n- Наложение запросов (overlap)\\n\\n- Если запрос идёт 3 секунды, а interval = 1 секунда, то за 10 секунд накопится пачка запросов.\\n\\n- Непредсказуемая нагрузка\\n\\n- Сервер получает всплески, клиент — рост потребления памяти/сети.\\n\\n- Сложнее делать backoff\\n\\n- При ошибках или `429` нужно увеличивать паузу, а у `setInterval` это менее удобно (часто приходится `clearInterval` и ставить заново).\\n\\n- Drift и очередь событий\\n\\n- Если вкладка “тормозит” или поток занят, срабатывания могут сдвигаться и сгущаться.\\n\\nПочему setTimeout обычно лучше\\n\\n- Следующий тик после завершения\\n\\n- Запускаем таймер только когда закончили текущий запрос.\\n\\n- Гибкая задержка\\n\\n- Можно динамически менять интервал: нормальный режим, backoff при ошибках, пауза при скрытой вкладке.\\n\\nПример polling на setTimeout (без наложений)\\n\\nВывод\\n`setTimeout` для polling обычно предпочтительнее, потому что даёт контроль: нет параллельных запросов, проще backoff и понятнее управление частотой.","let stopped = false;\\n\\nasync function poll() {\\n  try {\\n    const res = await fetch(\\"/api/status\\");\\n    // обработка res...\\n    if (!stopped) setTimeout(poll, 1000);\\n  } catch (e) {\\n    // при ошибке можно увеличить интервал\\n    if (!stopped) setTimeout(poll, 3000);\\n  }\\n}\\n\\npoll();\\n// чтобы остановить:\\nstopped = true;"],[1240,"Примитивы (например, `number`, `string`, `boolean`) хранят “само значение”, и при присваивании копируется значение. Непримитивы (объекты, массивы, функции) хранятся как ссылка на объект в памяти, и при присваивании копируется ссылка. Поэтому изменения объекта через одну переменную видны в другой. Также сравнение объектов чаще сравнивает ссылки, а не содержимое.","Определение\\nПримитивный тип — значение, которое не является объектом и ведёт себя как “копируемое значение”.\\nНепримитивный тип — объект (включая массивы и функции), который передаётся как “ссылка на один и тот же объект”.\\nЧто относится к примитивам и непримитивам\\n\\n- Примитивы:\\n\\n- `string`\\n\\n- `number`\\n\\n- `boolean`\\n\\n- `null`\\n\\n- `undefined`\\n\\n- `bigint`\\n\\n- `symbol`\\n\\n- Непримитивы:\\n\\n- `object` (в т.ч. `{}`, `[]`, `new Date()`, `Map`, `Set`)\\n\\n- `function` (технически тоже объект)\\n\\nОсновное отличие: копия значения vs копия ссылки\\nПеред перечислением важно запомнить идею: у примитива “нет внутреннего состояния, на которое можно сослаться”, а у объекта есть.\\n\\n- Примитивы копируются “как есть”\\n\\n- Объекты копируются как ссылка на один и тот же объект\\n\\nКак это влияет на сравнение\\n\\n- Примитивы сравниваются по значению:\\n\\n- `5 === 5` → `true`\\n\\n- Объекты сравниваются по ссылке:\\n\\nПрактический вывод\\n\\n- Если вы хотите “безопасно менять данные”, объекты нужно копировать (например, через spread или `structuredClone`).\\n\\n- Если вы храните состояние в UI (React/Redux), важно помнить: изменение вложенного объекта без создания новой ссылки часто ломает корректное обновление.","let a = 10;\\nlet b = a;\\nb = 20;\\n\\nconsole.log(a); // 10\\nconsole.log(b); // 20\\n\\nconst obj1 = { x: 1 };\\nconst obj2 = obj1;\\n\\nobj2.x = 2;\\n\\nconsole.log(obj1.x); // 2\\nconsole.log(obj2.x); // 2\\n\\nconst a = { x: 1 };\\nconst b = { x: 1 };\\nconst c = a;\\n\\nconsole.log(a === b); // false (разные объекты)\\nconsole.log(a === c); // true (одна и та же ссылка)"],[1241,"Когда вы присваиваете объект одной переменной другой, копируется не объект, а ссылка на него. В итоге обе переменные указывают на один и тот же объект в памяти. Если изменить объект через одну переменную, изменения будут видны через другую. Чтобы получить независимую копию, нужно явно копировать объект.","Определение\\nПрисваивание объекта — операция, которая копирует ссылку на объект, а не создаёт новый объект.\\nЧто происходит при присваивании\\nСначала скажем словами: после `b = a` у вас не два объекта, а один объект и две переменные, которые на него смотрят.\\n\\n- Пример “алиаса”\\n\\n- Переприсваивание меняет только ссылку в переменной\\n\\nКак сделать копию\\n\\n- Поверхностная копия (shallow copy) — копируются верхние поля, вложенные объекты остаются ссылками:\\n\\n- Глубокая копия (deep copy) — копируется всё дерево (когда это реально нужно):\\n\\nГде это важно на практике\\n\\n- React state:\\n\\n- Нельзя делать `state.obj.x = 2` и ожидать корректных обновлений.\\n\\n- Нужно создавать новый объект: `setState(prev => ({ ...prev, obj: { ...prev.obj, x: 2 } }))`.\\n\\n- Redux:\\n\\n- Иммутабельность нужна, чтобы изменения были “видны” через новые ссылки.\\n\\nВывод\\nПрисваивание объекта создаёт вторую ссылку на тот же объект, поэтому изменения “протекают” между переменными. Если нужна независимость — копируйте объект явно (shallow/deep в зависимости от структуры).","const a = { count: 0 };\\nconst b = a;\\n\\nb.count += 1;\\n\\nconsole.log(a.count); // 1\\n\\nconst a = { count: 0 };\\nlet b = a;\\n\\nb = { count: 100 };\\n\\nconsole.log(a.count); // 0\\nconsole.log(b.count); // 100\\n\\nconst a = { user: { name: \\"Ann\\" }, age: 20 };\\nconst b = { ...a };\\n\\nb.age = 21;\\nb.user.name = \\"Bob\\";\\n\\nconsole.log(a.age); // 20\\nconsole.log(a.user.name); // \\"Bob\\" (потому что user — общий)\\n\\nconst a = { user: { name: \\"Ann\\" } };\\nconst b = structuredClone(a);\\n\\nb.user.name = \\"Bob\\";\\nconsole.log(a.user.name); // \\"Ann\\""],[1242,"Практически все объекты в JavaScript в итоге наследуются от `Object.prototype`. Это “базовый” объект в прототипной цепочке, где находятся общие методы (`toString`, `hasOwnProperty` и т.д.). Сам `Object.prototype` обычно наследуется от `null`, то есть это конец цепочки. Примитивы напрямую не объекты, но при обращении к методам часто временно “оборачиваются” в объект-обёртку.","Определение\\nПрототипная цепочка — это механизм, где объект может искать свойства и методы не только в себе, но и в своём прототипе (`[[Prototype]]`).\\nБазовая идея наследования\\nПеред перечислением: когда вы пишете `obj.toString()`, JS ищет `toString` сначала в `obj`, потом в его прототипе, потом выше — пока не найдёт или не упрётся в конец цепочки.\\n\\n- Для обычных объектов конечная “база” — `Object.prototype`\\n\\n- А “последний шаг” после `Object.prototype` — это `null`\\n\\nПример проверки цепочки\\n\\nА как же массивы, функции, даты\\nУ них есть свои прототипы, но цепочка всё равно обычно приходит к `Object.prototype`.\\n\\n- Массив:\\n\\nВажный нюанс про примитивы\\nПримитивы сами по себе не наследуются как объекты, но методы у них появляются через временную “обёртку”:\\n\\n- `\\"hi\\".toUpperCase()` работает, потому что строка временно превращается в объект `String`.\\n\\nВывод\\nБольшинство объектов в JS в итоге наследуются от `Object.prototype`, а конец прототипной цепочки — `null`. Это объясняет, почему базовые методы доступны почти “везде”.","const obj = { a: 1 };\\n\\nconsole.log(Object.getPrototypeOf(obj) === Object.prototype); // true\\nconsole.log(Object.getPrototypeOf(Object.prototype)); // null\\n\\nconst arr = [];\\nconsole.log(Object.getPrototypeOf(arr) === Array.prototype); // true\\nconsole.log(Object.getPrototypeOf(Array.prototype) === Object.prototype); // true"],[1243,"В конце прототипной цепочки находится `null`. Обычно последний “реальный” объект в цепочке — это `Object.prototype`, а его прототип равен `null`. Когда поиск свойства доходит до `null`, JavaScript прекращает поиск и возвращает `undefined`. Это и есть финальная точка любого прототипного поиска.","Определение\\nКонец прототипной цепочки — это момент, когда у объекта больше нет прототипа, и дальнейший поиск свойств невозможен.\\nКак устроен поиск свойства\\nСначала опишем словами: при обращении к свойству JavaScript двигается “вверх” по цепочке прототипов.\\n\\n- Проверяется сам объект\\n\\n- Проверяется его прототип\\n\\n- Проверяются прототипы прототипов\\n\\n- Поиск останавливается, когда прототип равен `null`\\n\\nЧто именно является концом цепочки\\n\\n- Для большинства объектов:\\n\\n- Последний объект — `Object.prototype`\\n\\n- Его прототип — `null`\\n\\n- Это можно проверить явно:\\n\\nЧто происходит, если свойство не найдено\\nЕсли свойство не найдено ни в одном объекте цепочки:\\n\\n- JavaScript доходит до `null`\\n\\n- Поиск прекращается\\n\\n- Возвращается `undefined`\\n\\nПрактическое значение\\n\\n- Понимание конца цепочки помогает:\\n\\n- Объяснить, почему свойство “пропадает”\\n\\n- Избежать ошибок при доступе к вложенным методам\\n\\n- Это важно при:\\n\\n- Отладке\\n\\n- Понимании `hasOwnProperty`\\n\\n- Работе с кастомным наследованием\\n\\nВывод\\nКонцом прототипной цепочки всегда является `null`, а последний объект перед ним — `Object.prototype`. Это гарантирует, что поиск свойств в JavaScript всегда конечен.","const obj = {};\\n\\nconsole.log(Object.getPrototypeOf(obj)); // Object.prototype\\nconsole.log(Object.getPrototypeOf(Object.prototype)); // null"],[1244,"Строки — это примитивы, но при обращении к методу JavaScript временно оборачивает строку в объект `String`. Этот объект содержит методы, определённые в `String.prototype`. После вызова метода временный объект сразу удаляется. Поэтому методы у строк выглядят как “родные”, хотя сами строки объектами не являются.","Определение\\nАвтоупаковка (autoboxing) — это механизм, при котором примитив временно превращается в объект-обёртку для доступа к методам.\\nЧто происходит при вызове метода у строки\\nПеред перечислением важно понять общий алгоритм, который выполняет движок JavaScript.\\n\\n- Есть примитив:\\n\\n- При вызове метода:\\n\\n- JavaScript:\\n\\n- Создаёт временный объект `new String(\\"hello\\")`\\n\\n- Ищет метод в `String.prototype`\\n\\n- Вызывает метод\\n\\n- Удаляет временный объект\\n\\nГде хранятся методы строк\\n\\n- Методы не лежат “в строке”\\n\\n- Они находятся в `String.prototype`\\n\\n- Это можно проверить:\\n\\nПочему это сделано именно так\\n\\n- Примитивы остаются лёгкими и простыми\\n\\n- Методы не дублируются для каждого значения\\n\\n- Сохраняется единая модель прототипного наследования\\n\\nПрактический нюанс\\nПопытка записать свойство в строку не работает:\\n\\nЭто происходит потому, что объект-обёртка временный и сразу уничтожается.\\nВывод\\nМетоды у строк появляются благодаря автоупаковке: примитив временно превращается в объект `String`, методы берутся из `String.prototype`, а затем объект исчезает.","const str = \\"hello\\";\\n\\nstr.toUpperCase();\\n\\nconsole.log(String.prototype.toUpperCase !== undefined); // true\\n\\nconst str = \\"hi\\";\\nstr.test = 123;\\n\\nconsole.log(str.test); // undefined"],[1245,"`5.toString()` не работает, потому что JavaScript воспринимает точку как часть числового литерала. В результате парсер “ждёт” дробную часть числа, а не вызов метода. Вариант `5..toString()` работает, потому что первая точка завершает число, а вторая используется для доступа к методу. После этого число временно оборачивается в объект `Number`, и метод успешно вызывается.","Определение\\nЧисловой литерал — это способ записи числа прямо в коде (`5`, `3.14`, `10`).\\nВ чём проблема с `5.toString()`\\nСначала опишем, как это видит парсер.\\n\\n- Запись:\\n\\n- Парсер читает это как:\\n\\n- начало числа `5.`\\n\\n- ожидание дробной части\\n\\n- В итоге возникает синтаксическая ошибка\\n\\nПочему `5..toString()` работает\\nЗдесь поведение другое:\\n\\n- Первая точка завершает число:\\n\\n- Вторая точка — оператор доступа к свойству\\n\\n- Далее срабатывает автоупаковка:\\n\\n- создаётся временный `Number`\\n\\n- метод берётся из `Number.prototype`\\n\\nАльтернативные корректные способы\\nЕсть несколько читаемых и безопасных вариантов:\\n\\n- Через скобки:\\n\\n- Через переменную:\\n\\n- Через `Number`:\\n\\nПрактический вывод\\n\\n- `5..toString()` — допустимо, но выглядит странно\\n\\n- На практике используют скобки или переменную\\n\\n- Важно понимать, что проблема здесь в синтаксисе, а не в отсутствии метода\\n\\nВывод\\n`5.toString()` не работает из-за правил разбора числовых литералов, а `5..toString()` работает, потому что явно отделяет число от оператора доступа к методу и позволяет автоупаковке выполнить свою работу.","5.toString()\\n\\n5.\\n\\nconsole.log(5..toString()); // \\"5\\"\\n\\n(5).toString();\\n\\nconst n = 5;\\nn.toString();\\n\\nNumber(5).toString();"],[1246,"Чтобы корректно вызвать метод у числового литерала, нужно явно отделить число от оператора доступа к методу. Чаще всего используют скобки `(5).toString()` или предварительно сохраняют число в переменную. Это позволяет JavaScript корректно распарсить код и временно обернуть число в объект `Number`. После этого метод вызывается без ошибок.","Определение\\nВызов метода у числового литерала — это обращение к методу из `Number.prototype` у примитивного значения типа `number`.\\nПочему нельзя писать просто `5.toString()`\\nСначала важно понять причину проблемы. Парсер JavaScript воспринимает точку после числа как часть дробного числа, а не как доступ к свойству.\\n\\n- `5.toString()` — ошибка парсинга\\n\\n- `5.` — допустимый числовой литерал\\n\\n- Поэтому требуется явное разделение\\n\\nКорректные способы вызова\\nСуществует несколько читаемых и безопасных вариантов.\\n\\n- Через скобки:\\n\\n- Через переменную:\\n\\n- Через конструктор `Number`:\\n\\n- Через двойную точку (редко используется):\\n\\nЧто происходит под капотом\\n\\n- Число остаётся примитивом\\n\\n- JavaScript временно создаёт объект `Number`\\n\\n- Метод берётся из `Number.prototype`\\n\\n- После вызова временный объект уничтожается\\n\\nПрактический вывод\\n\\n- В реальном коде предпочтительны скобки или переменная\\n\\n- `5..toString()` допустим, но снижает читаемость\\n\\n- Важно понимать, что это вопрос синтаксиса, а не ограничение типов\\n\\nВывод\\nМетоды у числовых литералов вызываются корректно, если явно отделить число от оператора доступа к свойству. Это запускает автоупаковку и позволяет использовать методы `Number.prototype`.","(5).toString();\\n\\nconst n = 5;\\nn.toString();\\n\\nNumber(5).toString();\\n\\n5..toString();"],[1247,"Ключами обычного объекта могут быть только строки и `Symbol`. Если использовать другие типы (числа, объекты, boolean), они автоматически приводятся к строке. Это происходит неявно и может приводить к неожиданным конфликтам ключей. `Symbol` — единственный тип, который не приводится к строке.","Определение\\nКлюч объекта — это идентификатор свойства, по которому значение хранится и извлекается из объекта.\\nКакие типы поддерживаются напрямую\\nПеред перечислением важно подчеркнуть: не всё, что вы пишете в квадратных скобках, реально остаётся своим типом.\\n\\n- Строки (`string`)\\n\\n- Символы (`symbol`)\\n\\nЧто происходит с другими типами\\nВсе остальные типы приводятся к строке.\\n\\n- Числа:\\n\\n- Boolean:\\n\\n- Объекты:\\n\\nПочему `symbol` — особый случай\\n\\n- `Symbol` не приводится к строке\\n\\n- Каждый `Symbol` уникален\\n\\n- Ключи-символы не участвуют в обычных перечислениях (`Object.keys`)\\n\\nПрактический нюанс\\nЕсли нужны ключи “любого типа” без приведения:\\n\\n- Используйте `Map`\\n\\n- Там ключами могут быть объекты, функции и примитивы без преобразования\\n\\nВывод\\nВ обычных объектах реальными ключами являются только строки и `Symbol`. Все остальные типы неявно приводятся к строке, что важно учитывать при проектировании структуры данных.","const obj = {\\n  name: \\"Alex\\"\\n};\\n\\nconst sym = Symbol(\\"id\\");\\nobj[sym] = 123;\\n\\nconst obj = {};\\nobj[1] = \\"one\\";\\n\\nconsole.log(Object.keys(obj)); // [\\"1\\"]\\n\\nobj[true] = \\"yes\\"; // ключ   rue\\"\\n\\nconst key = {};\\nobj[key] = \\"value\\";\\n\\nconsole.log(Object.keys(obj)); // [\\"[object Object]\\"]"],[1248,"Ключи объекта приводятся к строке, потому что объект в JavaScript изначально задумывался как словарь “строка → значение”. Такое решение упрощает модель данных и делает доступ к свойствам быстрым и предсказуемым. Это поведение закреплено в спецификации языка. Исключением являются `Symbol`, добавленные позже.","Определение\\nПриведение ключей к строке — это автоматическое преобразование значения ключа в строку при использовании его в объекте.\\nИсторическая причина\\nСначала объяснение словами. JavaScript создавался как простой язык для браузеров, и объект рассматривался как ассоциативный массив со строковыми ключами.\\n\\n- Простая реализация\\n\\n- Быстрый доступ к свойствам\\n\\n- Минимальная сложность модели\\n\\nКак работает приведение\\nКогда вы пишете:\\n\\nJavaScript делает следующее:\\n\\n- Если `key` — `symbol`, он используется напрямую\\n\\n- Во всех остальных случаях вызывается приведение к строке\\n\\nПроблемы, которые это может вызвать\\n\\n- Коллизии ключей:\\n\\n- Неочевидные ключи вида `\\"[object Object]\\"`\\n\\nПочему это важно для React и фронтенда\\n\\n- При работе с данными из API\\n\\n- При динамическом формировании объектов\\n\\n- При выборе между `Object` и `Map`\\n\\nВывод\\nКлючи объектов приводятся к строке из-за архитектурных решений JavaScript и требований спецификации. Если нужно хранить данные с ключами любого типа без преобразований, стоит использовать `Map`.","obj[key] = value;\\n\\nconst obj = {};\\nobj[10] =   en\\";\\n\\nconsole.log(obj[\\"10\\"]); //   en\\"\\n\\nconst obj = {};\\nobj[1] = \\"a\\";\\nobj[\\"1\\"] = \\"b\\";\\n\\nconsole.log(obj[1]); // \\"b\\""],[1249,"`debounce` — это техника, при которой функция выполняется только спустя заданное время после последнего вызова. Если за это время функция вызывается снова, таймер сбрасывается. В итоге выполнение происходит один раз, когда события “затихли”. Это помогает уменьшить количество лишних операций.","Определение\\nDebounce — это ограничение частоты вызова функции так, чтобы она выполнялась только после паузы в событиях.\\nКак работает debounce\\nСначала опишем идею словами: функция “ждёт”, пока события перестанут происходить.\\n\\n- Происходит вызов функции\\n\\n- Запускается таймер\\n\\n- Если вызов повторяется до истечения таймера:\\n\\n- таймер сбрасывается\\n\\n- Функция выполняется только после паузы\\n\\nПростой пример\\n\\nТипичные кейсы использования\\n\\n- Поиск по мере ввода текста\\n\\n- Валидация формы\\n\\n- Автосохранение\\n\\n- Запросы к API при изменении фильтров\\n\\nПрактический нюанс\\nDebounce почти всегда используется там, где важно последнее действие, а не каждое промежуточное.\\nВывод\\nDebounce позволяет вызывать функцию только после того, как поток событий прекратился, снижая нагрузку и количество лишних операций.","function debounce(fn, delay) {\\n  let timer;\\n\\n  return function (...args) {\\n    clearTimeout(timer);\\n    timer = setTimeout(() => {\\n      fn.apply(this, args);\\n    }, delay);\\n  };\\n}"],[1250,"`debounce` выполняет функцию только после паузы в событиях, а `throttle` — не чаще одного раза за заданный интервал времени. Debounce подходит, когда важен финальный результат, а throttle — когда важна регулярность выполнения. Эти подходы решают похожие задачи, но применяются в разных сценариях. Выбор зависит от поведения, которое ожидается от интерфейса.","Определение\\nThrottle — это техника, при которой функция может выполняться не чаще одного раза за определённый интервал времени.\\nКлючевое различие подходов\\nПеред перечислением важно понимать основную идею.\\n\\n- Debounce:\\n\\n- выполняется после окончания серии событий\\n\\n- игнорирует промежуточные вызовы\\n\\n- Throttle:\\n\\n- выполняется регулярно\\n\\n- ограничивает максимальную частоту вызовов\\n\\nПример на словах\\n\\n- Ввод текста:\\n\\n- debounce → запрос уйдёт после того, как пользователь перестал печатать\\n\\n- Скролл страницы:\\n\\n- throttle → обработчик срабатывает, например, раз в 100 мс\\n\\nПочему нельзя заменить одно другим\\n\\n- Debounce может “никогда не сработать”, если события идут непрерывно\\n\\n- Throttle гарантирует периодическое выполнение\\n\\n- Неправильный выбор ухудшает UX\\n\\nПрактический вывод\\n\\n- Debounce — формы, поиск, фильтры\\n\\n- Throttle — скролл, resize, drag\\n\\nВывод\\nDebounce и throttle решают одну задачу — ограничение частоты, но делают это по-разному. Выбор зависит от того, нужен ли финальный результат или регулярное выполнение.",null],[1251,"Debounce используется, когда события происходят часто, но результат нужен только после того, как пользователь закончил действие. Типичный пример — поиск по вводу текста. Он помогает снизить нагрузку на приложение и сервер. Это делает интерфейс более отзывчивым и предсказуемым.","Типичные сценарии\\nПеред перечислением важно отметить: debounce применяют там, где важен последний ввод, а не процесс.\\n\\n- Поиск и автокомплит\\n\\n- Фильтрация списков\\n\\n- Автосохранение данных\\n\\n- Валидация полей формы\\n\\n- Отправка запросов при изменении параметров\\n\\nПример с запросом\\n\\nЧто будет без debounce\\n\\n- Запрос отправляется на каждый символ\\n\\n- Сервер перегружается\\n\\n- Интерфейс может “дёргаться”\\n\\nСвязь с React\\n\\n- Часто используется вместе с `useEffect`\\n\\n- Помогает избежать лишних ререндеров и запросов\\n\\n- Упрощает контроль над побочными эффектами\\n\\nВывод\\nDebounce применяется в ситуациях с частыми событиями, когда результат нужен только после паузы. Это один из базовых инструментов оптимизации UI и сетевых запросов.","const debouncedSearch = debounce((query) => {\\n  fetch(`/api/search?q=${query}`);\\n}, 300);"],[1252,"`Array.prototype.filter` создаёт новый массив, включающий только те элементы, для которых callback-функция вернула `true`. Исходный массив при этом не изменяется. Callback вызывается для каждого элемента массива по порядку. Метод часто используется для отбора данных по условию.","Определение\\nArray.prototype.filter — это метод массива, который отбирает элементы по условию и возвращает новый массив.\\nКак работает filter\\nСначала опишем общую идею: `filter` проходит по массиву и проверяет каждый элемент.\\n\\n- Метод принимает callback-функцию\\n\\n- Callback вызывается для каждого элемента\\n\\n- Если callback возвращает `true`:\\n\\n- элемент попадает в новый массив\\n\\n- Если `false`:\\n\\n- элемент пропускается\\n\\nБазовый пример\\n\\nАргументы callback-функции\\nCallback получает:\\n\\n- Текущий элемент\\n\\n- Индекс элемента\\n\\n- Исходный массив\\n\\nВажное свойство: неизменяемость\\n\\n- Исходный массив не меняется\\n\\n- `filter` всегда возвращает новый массив\\n\\n- Это особенно важно при работе с состоянием в React\\n\\nПрактический вывод\\n`filter` — основной инструмент для работы со списками данных, когда нужно отобрать элементы без изменения исходного массива.\\nВывод\\n`Array.prototype.filter` последовательно проверяет элементы массива и формирует новый массив на основе логического условия.","const numbers = [1, 2, 3, 4, 5];\\n\\nconst even = numbers.filter(n => n % 2 === 0);\\n\\nconsole.log(even); // [2, 4]\\n\\nnumbers.filter((value, index, array) => {\\n  return value > 2;\\n});"],[1253,"`this` внутри callback-функции `filter` может быть `undefined`, потому что callback вызывается как обычная функция, а не как метод объекта. В строгом режиме (`\\"use strict\\"`) значение `this` в таких функциях равно `undefined`. Контекст не “передаётся автоматически” внутрь callback. Поэтому `this` там часто оказывается не тем, что ожидают.","Определение\\nКонтекст выполнения (`this`) — это объект, к которому привязана функция в момент вызова.\\nКак вызывается callback в filter\\nВажно сначала понять, как именно вызывается callback.\\n\\n- `filter` вызывает callback сам\\n\\n- Callback не привязан к объекту\\n\\n- Вызов выглядит концептуально так:\\n\\nПоведение this\\n\\n- В обычном режиме:\\n\\n- `this` может указывать на глобальный объект\\n\\n- В строгом режиме:\\n\\n- `this === undefined`\\n\\nПочему это часто вызывает ошибки\\n\\n- Ожидается `this` из внешнего объекта\\n\\n- Но callback — это отдельная функция\\n\\n- Контекст теряется\\n\\nСвязь с React\\n\\n- В функциональных компонентах `this` не используется\\n\\n- В классовых компонентах подобная ошибка встречается часто\\n\\n- Поэтому важно явно управлять контекстом\\n\\nВывод\\n`this` внутри callback `filter` может быть `undefined`, потому что callback вызывается без привязки к объекту, особенно в строгом режиме.","callback(element, index, array);\\n\\n\\"use strict\\";\\n\\n[1, 2, 3].filter(function (x) {\\n  console.log(this); // undefined\\n  return x > 1;\\n});"],[1254,"Корректный контекст в callback можно передать несколькими способами: использовать стрелочную функцию, явно привязать `this` через `bind` или передать второй аргумент в методы массива. Самый популярный и безопасный способ — стрелочные функции, так как они не имеют собственного `this`. Выбор подхода зависит от стиля кода и окружения.","Основная идея\\nПеред тем как перечислять способы, важно помнить: `this` зависит не от места объявления функции, а от способа её вызова.\\nСпособы передачи контекста\\n1. Стрелочная функция\\nСтрелочные функции не имеют собственного `this` и берут его из внешней области.\\n\\n2. Явная привязка через bind\\nМожно вручную привязать контекст.\\n\\n3. Второй аргумент filter\\nМетоды массивов принимают `thisArg`.\\n\\nКакой способ предпочтительнее\\n\\n- Стрелочные функции — современный стандарт\\n\\n- `bind` полезен в старом коде\\n\\n- `thisArg` встречается реже, но важно знать для собеседований\\n\\nВывод\\nКонтекст в callback массива нужно передавать явно. Самый простой и читаемый способ — использовать стрелочные функции, которые наследуют `this` из внешней области.","const obj = {\\n  limit: 2,\\n  filterNumbers(arr) {\\n    return arr.filter(n => n > this.limit);\\n  }\\n};\\n\\nconst obj = {\\n  limit: 2\\n};\\n\\n[1, 2, 3].filter(function (n) {\\n  return n > this.limit;\\n}.bind(obj));\\n\\nconst obj = { limit: 2 };\\n\\n[1, 2, 3].filter(function (n) {\\n  return n > this.limit;\\n}, obj);"],[1255,"`Microtask` — это задачи с более высоким приоритетом, которые выполняются сразу после текущего стека вызовов. `Macrotask` — задачи с обычным приоритетом, которые обрабатываются по очереди между рендерами. Promise и `queueMicrotask` создают microtask, а `setTimeout` и события DOM — macrotask. Разница влияет на порядок выполнения и рендер.","Определение\\nMicrotask — задача, которая выполняется сразу после завершения текущего синхронного кода.\\nMacrotask — задача, которая попадает в очередь событий и выполняется в следующем цикле event loop.\\nAPI, создающие microtask\\nСначала перечислим задачи с высоким приоритетом.\\n\\n- `Promise.then / catch / finally`\\n\\n- `queueMicrotask`\\n\\n- `MutationObserver`\\n\\nAPI, создающие macrotask\\nТеперь задачи с обычным приоритетом.\\n\\n- `setTimeout`\\n\\n- `setInterval`\\n\\n- `setImmediate` (в среде Node.js)\\n\\n- DOM-события (`click`, `scroll`)\\n\\n- `requestAnimationFrame` (особый случай, перед рендером)\\n\\nПорядок выполнения\\nОбщая схема выглядит так:\\n\\n- Выполняется синхронный код\\n\\n- Выполняются все microtask\\n\\n- Выполняется рендер\\n\\n- Выполняется одна macrotask\\n\\nПрактический вывод\\n\\n- Microtask имеют приоритет над macrotask\\n\\n- Неправильное использование Promise может “отодвигать” рендер\\n\\n- Это важно при оптимизации UI\\n\\nВывод\\nMicrotask и macrotask — разные очереди с разным приоритетом. Знание API, которые их создают, помогает предсказывать порядок выполнения кода.","Promise.resolve().then(() => {\\n  console.log(\\"microtask\\");\\n});\\n\\nsetTimeout(() => {\\n  console.log(\\"macrotask\\");\\n}, 0);"],[1256,"Да, микрозадачи могут заблокировать рендер браузера. Браузер выполняет все microtask перед тем, как перейти к рендеру. Если microtask постоянно добавляют новые microtask, рендер откладывается. Это состояние называется starvation.","Как браузер решает, когда рендерить\\nПеред перечислением важно понять порядок шагов.\\n\\n- Выполняется текущий JavaScript\\n\\n- Выполняются все microtask\\n\\n- Только после этого возможен рендер\\n\\nКак возникает блокировка\\nЕсли внутри microtask создаётся новая microtask:\\n\\n- Очередь microtask никогда не становится пустой\\n\\n- Браузер не доходит до шага рендера\\n\\n- UI “зависает”\\n\\nПочему macrotask так не делают\\n\\n- После каждой macrotask браузер может выполнить рендер\\n\\n- Macrotask не имеют приоритета над рендером\\n\\n- Поэтому `setTimeout` безопаснее для долгих циклов\\n\\nПрактическое значение для фронтенда\\n\\n- Осторожно с рекурсивными Promise\\n\\n- Не делайте тяжёлые вычисления в microtask\\n\\n- Для длительных процессов используйте:\\n\\n- `setTimeout`\\n\\n- `requestIdleCallback`\\n\\n- Web Workers\\n\\nВывод\\nMicrotask могут заблокировать рендер, если очередь никогда не опустошается. Это редкая, но важная ловушка, которую любят спрашивать на собеседованиях.","function loop() {\\n  Promise.resolve().then(loop);\\n}\\n\\nloop();"],[1257,"Аналог `Promise.all` можно реализовать, создав новый Promise и вручную отслеживая выполнение всех переданных Promise. Нужно сохранить результаты в массив в правильном порядке и считать количество завершённых операций. Если хотя бы один Promise отклоняется, итоговый Promise должен сразу перейти в состояние rejected. Это демонстрирует понимание базовой модели Promise.","Определение\\nPromise.all — это метод, который возвращает Promise, резолвящийся массивом результатов, когда все входные Promise успешно завершились.\\nОсновные требования к реализации\\nПеред кодом важно понимать условия.\\n\\n- Сохранять порядок результатов\\n\\n- Резолвиться только после выполнения всех Promise\\n\\n- Реджектиться при первой ошибке\\n\\n- Поддерживать не-Promise значения\\n\\nУпрощённая реализация\\n\\nПочему важен Promise.resolve\\n\\n- Позволяет обрабатывать обычные значения\\n\\n- Делает поведение совместимым с `Promise.all`\\n\\n- Упрощает логику\\n\\nГде это полезно\\n\\n- Понимание внутреннего устройства Promise\\n\\n- Собеседования уровня Middle+\\n\\n- Отладка сложных асинхронных сценариев\\n\\nВывод\\nРучная реализация `Promise.all` показывает глубокое понимание асинхронности: очередей задач, обработки ошибок и синхронизации нескольких Promise.","function promiseAll(promises) {\\n  return new Promise((resolve, reject) => {\\n    const results = [];\\n    let completed = 0;\\n\\n    promises.forEach((p, index) => {\\n      Promise.resolve(p)\\n        .then(value => {\\n          results[index] = value;\\n          completed += 1;\\n\\n          if (completed === promises.length) {\\n            resolve(results);\\n          }\\n        })\\n        .catch(reject);\\n    });\\n\\n    if (promises.length === 0) {\\n      resolve([]);\\n    }\\n  });\\n}"],[1258,"`this` не “лежит” внутри функции навсегда — он определяется в момент вызова. Если функция вызвана как метод объекта (`obj.fn()`), то `this` обычно будет указывать на `obj`. Если вынести метод в переменную и вызвать как обычную функцию (`const f = obj.fn; f()`), то `this` потеряется: в `strict mode` станет `undefined`, а без `strict mode` — глобальным объектом (в браузере это `window`). У стрелочной функции нет своего `this`, она берет его из внешнего окружения.","Определение\\n`this` — это контекст вызова функции, который определяется call site (местом/способом вызова), а не местом объявления функции.\\nКак понять, что будет в `this`\\nОбычно достаточно посмотреть на форму вызова:\\n\\n- Вызов как метод: `obj.method()`\\n\\n- `this` → `obj`\\n\\n- Обычный вызов функции: `fn()`\\n\\n- `this` → `undefined` в `strict mode`\\n\\n- `this` → глобальный объект без `strict mode`\\n\\n- Стрелочная функция: `() => {}`\\n\\n- `this` не меняется при вызове, берется снаружи\\n\\nПримеры\\n1) Метод объекта\\n\\n2) Потеря контекста при “отрыве” метода\\n\\n3) Стрелка внутри метода (берет `this` снаружи)\\n\\nПрактический смысл\\n\\n- Потеря `this` часто случается, когда:\\n\\n- передаем метод как колбэк (`onClick={obj.method}`)\\n\\n- деструктурируем метод (`const { method } = obj`)\\n\\n- используем `setTimeout(obj.method, 0)`\\n\\nВывод\\n`this` зависит от формы вызова. Чтобы избежать неожиданностей, следи за тем, вызываешь ли ты функцию как `obj.method()` или как `fn()`; для колбэков часто используют `bind`, стрелки или обертку-функцию.","\'use strict\';\\n\\nconst user = {\\n  name: \'Ann\',\\n  show() {\\n    console.log(this.name);\\n  },\\n};\\n\\nuser.show(); // \\"Ann\\" (this = user)\\n\\n\'use strict\';\\n\\nconst user = {\\n  name: \'Ann\',\\n  show() {\\n    console.log(this);\\n  },\\n};\\n\\nconst f = user.show;\\nf(); // undefined (strict mode)\\n\\n\'use strict\';\\n\\nconst user = {\\n  name: \'Ann\',\\n  showLater() {\\n    setTimeout(() => {\\n      console.log(this.name);\\n    }, 0);\\n  },\\n};\\n\\nuser.showLater(); // \\"Ann\\""],[1259,"Контекст можно привязать через `bind`, `call`, `apply`, через обертку (`() => fn.call(obj)`), или используя стрелочную функцию там, где нужен `this` извне. `bind` возвращает новую функцию с “приклеенным” `this` и (опционально) аргументами. Стрелочная функция не имеет собственного `this` и берет `this` из внешней области видимости, поэтому ее нельзя “перебиндить”. `bind` удобен, когда нужна повторно используемая функция с фиксированным `this`, а стрелка — когда важно сохранить внешний контекст (например, внутри метода при передаче колбэка).","Определение\\nПривязка контекста — это способ сделать так, чтобы внутри функции `this` был предсказуемым, даже если функция вызвана “не как метод”.\\nСпособы привязать/контролировать `this`\\n\\n- `bind`\\n\\n- фиксирует `this`\\n\\n- может частично “подставить” аргументы (каррирование)\\n\\n- возвращает новую функцию\\n\\n- `call` / `apply`\\n\\n- вызывают функцию сразу\\n\\n- задают `this` для конкретного вызова\\n\\n- разница: `call(fn, a, b)`, `apply(fn, [a, b])`\\n\\n- Обертка-стрелка\\n\\n- сохраняет внешний `this`\\n\\n- позволяет явно вызвать с нужным контекстом\\n\\nЧем `bind` отличается от стрелочной функции\\n1) `bind` “приклеивает” `this` к функции\\n\\n2) У стрелки `this` лексический и не меняется\\n\\n3) Важный нюанс: стрелку нельзя “перебиндить”\\n\\nПрактические случаи\\n\\n- `bind` полезен, когда:\\n\\n- нужно передать функцию как колбэк и не потерять `this`\\n\\n- нужна функция, которую будешь переиспользовать много раз\\n\\n- Стрелка полезна, когда:\\n\\n- пишешь колбэк внутри метода и хочешь сохранить `this` метода\\n\\n- не хочешь думать о `this` внутри вложенных функций\\n\\nВывод\\n`bind` создает новую функцию с фиксированным `this` и подходит для переиспользуемых колбэков. Стрелка не создает свой `this` и просто “наследует” внешний контекст — идеально для вложенных обработчиков внутри методов.","\'use strict\';\\n\\nfunction show() {\\n  console.log(this.name);\\n}\\n\\nconst user = { name: \'Ann\' };\\n\\nconst bound = show.bind(user);\\nbound(); // \\"Ann\\"\\n\\n\'use strict\';\\n\\nconst user = {\\n  name: \'Ann\',\\n  showLater() {\\n    const arrow = () => console.log(this.name);\\n    arrow();\\n  },\\n};\\n\\nuser.showLater(); // \\"Ann\\"\\n\\n\'use strict\';\\n\\nconst user1 = { name: \'Ann\' };\\nconst user2 = { name: \'Bob\' };\\n\\nconst arrow = () => console.log(this); // this берется снаружи (в модуле обычно undefined)\\nconst bound = arrow.bind(user1);\\n\\nbound(); // не станет user1"],[1260,"Hoisting — это когда объявления “обрабатываются” до выполнения кода. У `var` объявление поднимается и переменная сразу инициализируется как `undefined`, поэтому к ней можно обратиться до строки объявления (получишь `undefined`). У `let` и `const` тоже есть поднятие объявления, но до фактической инициализации переменная находится в TDZ (temporal dead zone), и доступ к ней до объявления вызывает `ReferenceError`. `const` дополнительно требует инициализацию сразу при объявлении.","Определение\\nHoisting — это поведение движка, при котором объявления (declaration) учитываются до выполнения кода. Важно: “поднимается” не значение, а факт объявления.\\n`var`: поднятие + инициализация `undefined`\\n\\n- `var` имеет функциональную область видимости (function scope)\\n\\n- при входе в функцию переменная существует и равна `undefined`\\n\\nПример:\\n\\nЭто эквивалентно по смыслу:\\n\\n`let` и `const`: поднятие объявления + TDZ\\n\\n- `let/const` имеют блочную область видимости (block scope)\\n\\n- переменная существует “в блоке”, но недоступна до строки объявления\\n\\n- попытка чтения до объявления → `ReferenceError`\\n\\nПример:\\n\\n`const` дополнительно\\n\\n- `const` нельзя объявить без значения\\n\\n- запрещено переназначение (но объект внутри может быть мутабельным)\\n\\nПример:\\n\\nПочему это важно в реальном коде\\n\\n- `var` может “маскировать” ошибки, возвращая `undefined` там, где ожидалась переменная\\n\\n- `let/const` дают более безопасное поведение: ошибка видна сразу\\n\\n- в циклах и блоках `let` часто предотвращает баги со “всплывающими” значениями\\n\\nВывод\\n`var` поднимается и инициализируется `undefined`, поэтому доступ “до объявления” возможен. `let/const` тоже поднимаются, но до инициализации находятся в TDZ, поэтому доступ до объявления приводит к `ReferenceError` — это делает код безопаснее.","\'use strict\';\\n\\nconsole.log(x); // undefined\\nvar x = 10;\\nconsole.log(x); // 10\\n\\n\'use strict\';\\n\\nvar x;           // объявление поднялось\\nconsole.log(x);  // undefined\\nx = 10;          // присваивание осталось на месте\\nconsole.log(x);\\n\\n\'use strict\';\\n\\nconsole.log(a); // ReferenceError\\nlet a = 10;\\n\\n\'use strict\';\\n\\nconst n = 1;\\n// n = 2; // TypeError: Assignment to constant variable"],[1261,"`let` и `const` поднимаются (hoisting есть), но не инициализируются сразу. До строки объявления переменная находится в TDZ, и любое обращение к ней приводит к `ReferenceError`. Это сделано намеренно, чтобы избежать скрытых багов, когда код читает переменную до ее логического объявления. В отличие от `var`, где до объявления возвращается `undefined`, `let/const` сразу сигнализируют об ошибке.","Определение\\nTDZ (temporal dead zone) — это промежуток времени от начала области видимости до строки инициализации переменной `let` или `const`, в котором переменная существует, но недоступна.\\nЧто происходит пошагово\\n\\n- Движок видит объявление `let/const` и резервирует имя в области видимости\\n\\n- До строки объявления переменная находится в TDZ\\n\\n- Любая попытка:\\n\\n- прочитать значение\\n\\n- использовать в выражении\\nприводит к `ReferenceError`\\n\\nПример с `let`\\n\\nДля сравнения с `var`\\n\\nПочему так сделали\\n\\n- Чтобы ошибки были явными, а не замаскированными\\n\\n- Чтобы код читался сверху вниз без “магии”\\n\\n- Чтобы уменьшить количество багов в сложных блоках и циклах\\n\\nОсобенность `const`\\n\\n- `const` тоже имеет TDZ\\n\\n- Требует инициализацию сразу\\n\\nВывод\\n`ReferenceError` при `let/const` — это защитный механизм. Он заставляет писать код, где переменные используются только после явного объявления, делая поведение предсказуемым.","\'use strict\';\\n\\nconsole.log(a); // ReferenceError\\nlet a = 10;\\n\\n\'use strict\';\\n\\nconsole.log(b); // undefined\\nvar b = 10;\\n\\n\'use strict\';\\n\\nconst x; // SyntaxError"],[1262,"`then` вызывается при успешном выполнении Promise, `catch` — при ошибке, а `finally` — всегда, независимо от результата. `finally` выполняется после `then` или `catch`, но не меняет значение, если сам не выбрасывает ошибку. Все эти колбэки выполняются как микротаски. Порядок строго следует цепочке.","Базовый порядок выполнения\\nPromise проходит одно из состояний:\\n\\n- `fulfilled` → вызываются `then`\\n\\n- `rejected` → вызываются `catch`\\n\\n- После этого всегда выполняется `finally`\\n\\nПример успешного выполнения\\n\\nПорядок вывода:\\n\\n- `then 1`\\n\\n- `finally`\\n\\nПример с ошибкой\\n\\nПорядок вывода:\\n\\n- `catch err`\\n\\n- `finally`\\n\\nВажный нюанс про `finally`\\n\\n- `finally` не получает результат\\n\\n- `finally` не изменяет цепочку, если:\\n\\n- не возвращает Promise\\n\\n- не выбрасывает ошибку\\n\\nВывод\\n`then` и `catch` обрабатывают результат или ошибку, а `finally` — это место для очистки ресурсов. Он выполняется всегда и не влияет на данные, если не делает этого явно.","Promise.resolve(1)\\n  .then(v => {\\n    console.log(\'then\', v);\\n    return v + 1;\\n  })\\n  .finally(() => {\\n    console.log(\'finally\');\\n  });\\n\\nPromise.reject(\'err\')\\n  .then(() => {\\n    console.log(\'then\');\\n  })\\n  .catch(e => {\\n    console.log(\'catch\', e);\\n  })\\n  .finally(() => {\\n    console.log(\'finally\');\\n  });\\n\\nPromise.resolve(1)\\n  .finally(() => 100)\\n  .then(v => console.log(v)); // 1"],[1263,"Да, внутри `then` можно выбросить ошибку с помощью `throw`. Такая ошибка автоматически превращает Promise в `rejected`. Она будет поймана ближайшим `catch` ниже по цепочке. По сути, `throw` внутри `then` эквивалентен `return Promise.reject(error)`.","Как работает ошибка в `then`\\nЕсли внутри `then`:\\n\\n- выбрасывается ошибка (`throw`)\\n\\n- возвращается `Promise.reject(...)`\\n\\nто цепочка переключается в режим ошибки.\\nПример с `throw`\\n\\nРезультат:\\n\\n- `catch` поймает ошибку `\\"boom\\"`\\n\\nЭквивалент через `reject`\\n\\nЧто если `catch` нет\\n\\n- Ошибка “провалится” дальше по цепочке\\n\\n- Если не будет ни одного `catch`, появится `UnhandledPromiseRejection`\\n\\nСвязь с `finally`\\n\\n- `finally` выполнится до следующего `catch`\\n\\n- Но ошибку он не поглотит, если сам не обработает ее\\n\\nВывод\\nОшибки внутри `then` — нормальная и ожидаемая практика. Они автоматически превращаются в `rejected` Promise и обрабатываются ближайшим `catch`, что делает цепочки Promise предсказуемыми и удобными.","Promise.resolve(1)\\n  .then(v => {\\n    throw new Error(\'boom\');\\n  })\\n  .catch(e => {\\n    console.log(e.message);\\n  });\\n\\nPromise.resolve(1)\\n  .then(() => {\\n    return Promise.reject(\'fail\');\\n  })\\n  .catch(e => console.log(e));"],[1264,"Технически достаточно одного `catch` в конце цепочки. Он перехватит любую ошибку, возникшую выше. Дополнительные `catch` нужны только тогда, когда ошибку нужно обработать локально и продолжить выполнение цепочки. Лишние `catch` без необходимости усложняют код и ухудшают читаемость.","Базовый принцип\\nPromise-цепочка распространяет ошибку вниз, пока не встретит `catch`.\\n\\n- Ошибка возникла в `then`\\n\\n- Все последующие `then` пропускаются\\n\\n- Выполняется первый подходящий `catch` ниже\\n\\nМинимально достаточный вариант\\n\\n- Любая ошибка из `fetchData`, `parse` или `save` попадет в `handleError`\\n\\nКогда имеет смысл несколько `catch`\\n\\n- Нужно обработать ошибку и продолжить цепочку\\n\\n- Разные этапы требуют разной логики обработки\\n\\nПример:\\n\\nВажный нюанс\\nЕсли `catch`:\\n\\n- обработал ошибку\\n\\n- вернул значение\\n\\nто цепочка снова считается успешной.\\nВывод\\nОдин `catch` в конце — стандарт и best practice. Несколько `catch` используют осознанно, когда нужно восстановиться после ошибки и продолжить выполнение.","fetchData()\\n  .then(parse)\\n  .then(save)\\n  .catch(handleError);\\n\\nfetchData()\\n  .catch(() => getFallbackData())\\n  .then(process)\\n  .catch(logError);"],[1265,"`let` в цикле создает новую переменную на каждую итерацию. Поэтому каждое замыкание “запоминает” свое значение. В отличие от `var`, где используется одна общая переменная на весь цикл, `let` предотвращает типичную ошибку с одинаковыми значениями. Это поведение заложено в спецификацию языка.","Что происходит с `let` в цикле\\nКогда используется `let`:\\n\\n- Каждая итерация цикла имеет свою область видимости\\n\\n- Переменная пересоздается заново\\n\\n- Замыкание захватывает уникальное значение\\n\\nПример с `let`\\n\\nРезультат:\\n\\n- `0`\\n\\n- `1`\\n\\n- `2`\\n\\nТо же самое с `var`\\n\\nРезультат:\\n\\n- `3`\\n\\n- `3`\\n\\n- `3`\\n\\nПочему так происходит\\n\\n- `var` имеет одну общую переменную\\n\\n- К моменту выполнения `setTimeout` цикл уже завершился\\n\\n- `let` создает новый binding на каждую итерацию\\n\\nПрактический эффект\\n\\n- `let` делает код безопаснее\\n\\n- не нужны IIFE или дополнительные обертки\\n\\n- поведение интуитивно ожидаемое\\n\\nВывод\\nРазные значения при `let` — это не “магия”, а результат блочной области видимости и корректной работы замыканий.","for (let i = 0; i < 3; i++) {\\n  setTimeout(() => {\\n    console.log(i);\\n  }, 0);\\n}\\n\\nfor (var i = 0; i < 3; i++) {\\n  setTimeout(() => {\\n    console.log(i);\\n  }, 0);\\n}"],[1266,"Функция-счётчик будет возвращать увеличивающееся значение при каждом вызове. Это возможно потому, что внутренняя функция сохраняет доступ к переменной из внешней функции. Такая переменная не уничтожается после выполнения, а продолжает жить в замыкании. Каждый вызов использует одно и то же сохраненное состояние.","Определение\\nЗамыкание — это функция, которая имеет доступ к переменным внешней области видимости даже после завершения выполнения этой области.\\nПример счётчика\\n\\nПоведение при вызовах\\n\\nЧто здесь важно\\n\\n- `count` создается один раз\\n\\n- Он недоступен извне напрямую\\n\\n- Все вызовы работают с одной и той же переменной\\n\\nПочему это полезно\\n\\n- Инкапсуляция состояния\\n\\n- Отсутствие глобальных переменных\\n\\n- Часто используется в:\\n\\n- счётчиках\\n\\n- кешировании\\n\\n- фабриках функций\\n\\nВывод\\nФункция-счётчик с замыканием возвращает возрастающее значение, потому что внутренняя функция сохраняет и изменяет состояние между вызовами.","function createCounter() {\\n  let count = 0;\\n\\n  return function () {\\n    count++;\\n    return count;\\n  };\\n}\\n\\nconst counter = createCounter();\\n\\ncounter(); // 1\\ncounter(); // 2\\ncounter(); // 3"],[1267,"Несколько вызовов функции могут ссылаться на одну и ту же переменную, если эта переменная находится во внешнем лексическом окружении. Замыкание не копирует значение переменной, а сохраняет ссылку на неё. Поэтому все функции, созданные в одном окружении, работают с одним и тем же состоянием. Изменение переменной в одном месте будет видно во всех остальных.","Определение\\nЛексическое окружение — это внутренняя структура JavaScript, в которой хранятся переменные, доступные функции в момент её создания.\\nКак возникает общая переменная\\n\\n- Внешняя функция создаёт переменную\\n\\n- Внутренняя функция использует эту переменную\\n\\n- Внешняя функция завершает выполнение\\n\\n- Переменная не удаляется, потому что на неё есть ссылка\\n\\nПример общей переменной\\n\\nПочему это одна и та же переменная\\n\\n- `value` создана один раз\\n\\n- `fn1` и `fn2` указывают на одну функцию\\n\\n- Замыкание содержит ссылку на одно окружение\\n\\nЧастый источник багов\\n\\n- Все вызовы работают с одним состоянием\\n\\n- Без явного контроля это может быть неожиданно\\n\\nВывод\\nЗамыкания работают со ссылкой на переменную, а не с её копией. Поэтому несколько вызовов могут изменять одно и то же состояние.","function createFn() {\\n  let value = 0;\\n\\n  return function () {\\n    value++;\\n    console.log(value);\\n  };\\n}\\n\\nconst fn1 = createFn();\\nconst fn2 = fn1;\\n\\nfn1(); // 1\\nfn2(); // 2\\n\\nlet shared = 0;\\n\\nfunction inc() {\\n  shared++;\\n}\\n\\ninc();\\ninc();"],[1268,"Временная сложность показывает, как растёт время выполнения алгоритма при увеличении входных данных. Обычно используют нотацию Big O, где учитывают только доминирующий фактор роста. Константы и малые члены отбрасываются. Основной фокус — на количестве операций в худшем случае.","Определение\\nВременная сложность (Big O) — это оценка роста количества операций алгоритма в зависимости от размера входных данных `n`.\\nБазовые шаги определения сложности\\n\\n- Определить, от чего зависит размер входа (`n`)\\n\\n- Посчитать количество операций\\n\\n- Оставить доминирующий член\\n\\n- Игнорировать константы\\n\\nПримеры\\nO(1) — константная\\n\\nO(n) — линейная\\n\\nO(n²) — квадратичная\\n\\nВажные нюансы\\n\\n- Вложенные циклы → умножение сложностей\\n\\n- Последовательные циклы → сложность складывается, но остаётся доминирующая\\n\\n- Чаще всего оценивают худший случай\\n\\nВывод\\nОпределение временной сложности — это умение абстрагироваться от деталей реализации и смотреть на рост количества операций при увеличении `n`.","arr[0];\\n\\nfor (let i = 0; i < arr.length; i++) {\\n  // операция\\n}\\n\\nfor (let i = 0; i < n; i++) {\\n  for (let j = 0; j < n; j++) {\\n    // операция\\n  }\\n}"],[1269,"Операции доступа к объекту-словарю (`Object` или `Map`) обычно имеют O(1). Это связано с использованием хеш-таблиц. Однако в худшем случае сложность может деградировать до O(n) из-за коллизий. На практике для большинства задач доступ считается константным.","Что такое объект-словарь\\nОбъект или `Map` часто используются как структура:\\n\\n- ключ → значение\\n\\n- быстрый доступ по ключу\\n\\nСредний случай\\n\\n- Ключ хешируется\\n\\n- По хешу находится ячейка\\n\\n- Доступ происходит за O(1)\\n\\nПример:\\n\\nХудший случай\\n\\n- Много ключей с одинаковым хешем\\n\\n- Возникают коллизии\\n\\n- Поиск может стать линейным → O(n)\\n\\nПочему на собеседованиях говорят O(1)\\n\\n- Современные реализации минимизируют коллизии\\n\\n- Худший случай редко учитывается\\n\\n- Важно понимать асимптотику, а не реализацию движка\\n\\nВывод\\nРешения с объектом-словарём в среднем работают за O(1), что делает их популярными для кешей, подсчётов и быстрых поисков.","const dict = {};\\ndict[\'id\'] = 10;\\nconsole.log(dict[\'id\']); // O(1)"],[1270,"Чтобы посчитать все элементы, включая вложенные массивы, нужно обойти структуру рекурсивно. Если элемент — массив, мы заходим внутрь и продолжаем подсчёт. Если это обычное значение, увеличиваем счётчик. Такой подход корректно учитывает любую глубину вложенности.","Общая идея решения\\nМассив может содержать:\\n\\n- Примитивные значения\\n\\n- Другие массивы\\n\\nПоэтому задача сводится к обходу дерева.\\nРекурсивный подход\\n\\nПример использования\\n\\nЧто здесь важно\\n\\n- Проверка через `Array.isArray`\\n\\n- Базовый случай — обычное значение\\n\\n- Рекурсия естественно отражает структуру данных\\n\\nСложность\\n\\n- Временная сложность: O(n), где `n` — общее количество элементов\\n\\n- Память: зависит от глубины вложенности (call stack)\\n\\nВывод\\nРекурсия — самый читаемый и универсальный способ посчитать элементы во вложенных массивах.","function countItems(arr) {\\n  let count = 0;\\n\\n  for (const item of arr) {\\n    if (Array.isArray(item)) {\\n      count += countItems(item);\\n    } else {\\n      count++;\\n    }\\n  }\\n\\n  return count;\\n}\\n\\ncountItems([1, [2, 3], [4, [5]]]); // 5"],[1271,"Задача решается либо рекурсией, либо итеративно через стек. Рекурсия проще, но может привести к `stack overflow` при глубокой вложенности. Итеративный подход со своим стеком безопаснее и подходит для бесконечной или очень глубокой структуры. На практике выбор зависит от ограничений на глубину данных.","Почему обычная рекурсия может быть опасна\\n\\n- Каждый вызов занимает место в call stack\\n\\n- При большой глубине возникает `RangeError`\\n\\n- Глубина данных не всегда контролируема\\n\\nИтеративное решение через стек\\n\\nПреимущества итеративного подхода\\n\\n- Нет риска переполнения стека вызовов\\n\\n- Контроль над памятью\\n\\n- Подходит для “бесконечной” глубины\\n\\nКогда достаточно рекурсии\\n\\n- Глубина ограничена\\n\\n- Код должен быть максимально читаемым\\n\\n- Нет строгих требований к отказоустойчивости\\n\\nВывод\\nДля неизвестной или потенциально бесконечной вложенности лучше использовать итеративный подход со стеком, а рекурсию — только при гарантированной глубине.","function countItems(arr) {\\n  let count = 0;\\n  const stack = [...arr];\\n\\n  while (stack.length) {\\n    const item = stack.pop();\\n\\n    if (Array.isArray(item)) {\\n      stack.push(...item);\\n    } else {\\n      count++;\\n    }\\n  }\\n\\n  return count;\\n}"],[1272,"Скрипты могут блокировать рендеринг, потому что браузер должен остановить парсинг HTML, загрузить и выполнить JavaScript. Это нужно, так как скрипт может изменить DOM или CSS. Пока синхронный скрипт не выполнен, браузер не продолжает строить DOM и не может корректно рендерить страницу. Атрибуты `defer` и `async` позволяют уменьшить или убрать эту блокировку.","JavaScript напрямую участвует в формировании страницы, поэтому браузер относится к нему как к потенциально опасному для рендера ресурсу.\\nОпределение\\nОпределение: Render-blocking script — это скрипт, который останавливает парсинг HTML и задерживает отрисовку страницы до момента своей загрузки и выполнения.\\nПочему браузер блокирует парсинг\\nБраузер не знает заранее, что сделает скрипт:\\n\\n- может изменить DOM (`document.write`, `appendChild`)\\n\\n- может читать стили и размеры элементов\\n\\n- может добавить новые стили или скрипты\\n\\nИз-за этого браузер обязан выполнить скрипт строго в том месте, где он встречается.\\nПоведение обычного `<script>`\\n\\n- Парсинг HTML останавливается.\\n\\n- Скрипт загружается (если внешний).\\n\\n- Скрипт выполняется.\\n\\n- Парсинг HTML продолжается.\\n\\nКак `defer` и `async` меняют ситуацию\\n\\n- `defer`\\n\\n- HTML парсится без остановки.\\n\\n- Скрипт выполняется после построения DOM.\\n\\n- Порядок выполнения сохраняется.\\n\\n- `async`\\n\\n- HTML парсится без остановки.\\n\\n- Скрипт выполняется сразу после загрузки.\\n\\n- Порядок выполнения не гарантирован.\\n\\nПрактический эффект\\n\\n- Синхронные скрипты → задержка `FCP`, “белый экран”.\\n\\n- `defer` → безопасный вариант для большинства приложений.\\n\\n- `async` → хорошо для независимых скриптов (метрики, реклама).\\n\\nВывод: скрипты блокируют рендеринг, потому что браузер обязан выполнить их до продолжения парсинга. Использование `defer` и `async` — ключ к ускорению загрузки страницы.","<script src=\\"app.js\\" defer><\/script>\\n<script src=\\"analytics.js\\" async><\/script>"],[1273,"Скрипты стоит загружать в конце `body`, когда они не используют `defer` или `async`. В этом случае браузер сначала строит DOM и отображает контент, а уже потом выполняет JavaScript. Это уменьшает задержку первой отрисовки и снижает риск ошибок из-за отсутствующих элементов в DOM. Такой подход особенно актуален для простых страниц и старых проектов.","Размещение скриптов — простой, но эффективный способ управления загрузкой.\\nИдея подхода\\nЕсли скрипт находится в конце `body`, браузер:\\n\\n- Сначала строит почти весь DOM.\\n\\n- Выполняет первую отрисовку.\\n\\n- Только потом загружает и выполняет JavaScript.\\n\\nПример\\n\\nВ этом случае `document.getElementById(\'app\')` гарантированно существует.\\nКогда это оправдано\\n\\n- нет возможности использовать `defer`\\n\\n- простой сайт без сложной сборки\\n\\n- legacy-код или сторонние скрипты\\n\\n- нужно быстро решить проблему блокировки\\n\\nОграничения подхода\\n\\n- не решает проблему порядка загрузки нескольких скриптов\\n\\n- не даёт гибкости, как `defer`\\n\\n- хуже масштабируется в больших приложениях\\n\\nСовременная альтернатива\\nСегодня чаще используют:\\n\\nЭто даёт тот же эффект, но чище и предсказуемее.\\nВывод: загрузка скриптов в конце `body` — рабочий способ уменьшить блокировку рендеринга, но в современных приложениях его чаще заменяет `defer`.","<body>\\n  <div id=\\"app\\"></div>\\n\\n  <script src=\\"app.js\\"><\/script>\\n</body>\\n\\n<script src=\\"app.js\\" defer><\/script>"],[1274,"`NaN` означает “не число” и по спецификации не равен ничему, включая самого себя. Это сделано для отражения ошибок вычислений, а не конкретного значения. Поэтому любое сравнение `NaN` через `==` или `===` возвращает `false`. Для проверки `NaN` используются специальные методы.","Поведение `NaN` кажется странным, но оно намеренное.\\nОпределение\\nОпределение: NaN — специальное числовое значение, обозначающее некорректный результат вычислений.\\nПочему так задумано\\n`NaN` не представляет конкретное число, а сигнализирует об ошибке:\\n\\n- деление строки на число\\n\\n- результат `0 / 0`\\n\\n- некорректные математические операции\\n\\nЕсли бы `NaN === NaN` было `true`, это могло бы скрывать ошибки в вычислениях.\\nПример\\n\\nКак правильно проверять NaN\\n\\n- `Number.isNaN(value)` — рекомендуемый способ\\n\\n- глобальная `isNaN()` — приводит типы и может вводить в заблуждение\\n\\nВывод: `NaN !== NaN`, потому что `NaN` — это маркер ошибки, а не обычное значение, и сравнение с ним всегда ложно.","const a = Number(\'abc\');\\n\\na === a; // false\\n\\nNumber.isNaN(NaN);      // true\\nNumber.isNaN(\'text\');  // false"],[1275,"При нестрогом сравнении `==` JavaScript пытается привести операнды к одному типу. Правила зависят от типов значений: строки могут превращаться в числа, `null` и `undefined` считаются равными друг другу, а объекты приводятся к примитивам. Эти преобразования не всегда очевидны, из-за чего сравнение может вести себя неожиданно. Именно поэтому в большинстве случаев рекомендуют использовать `===`.","Нестрогое сравнение подчиняется формальным, но сложным правилам.\\nОпределение\\nОпределение: Type coercion — автоматическое приведение типов JavaScript при выполнении операций, включая `==`.\\nОбщая логика сравнения `==`\\nПеред сравнением JavaScript может:\\n\\n- Привести строку к числу.\\n\\n- Привести объект к примитиву.\\n\\n- Особым образом обработать `null` и `undefined`.\\n\\nТипичные правила\\n\\n- Строка и число:\\n\\n- `null` и `undefined`:\\n\\n- Объект и примитив:\\n\\nОбъект сначала преобразуется в примитив.\\nПочему это опасно\\n\\n- сравнение выглядит “логичным”, но работает неявно\\n\\n- легко допустить ошибку в условиях\\n\\n- код сложнее читать и поддерживать\\n\\nРекомендация\\nИспользовать `===`, если нет явной причины полагаться на приведение типов.\\n\\nВывод: при `==` JavaScript автоматически приводит типы по набору правил, что делает поведение гибким, но потенциально опасным.","\'5\' == 5; // true\\n\\nnull == undefined; // true\\n\\n[1] == 1; // true\\n\\n\'5\' === 5; // false"],[1276,"В браузере глобальным контекстом выполнения является объект `window`. Все глобальные переменные и функции становятся его свойствами. Код, написанный вне функций и модулей, выполняется именно в этом контексте. Через глобальный контекст браузер предоставляет доступ к API, таким как `document`, `location` и `setTimeout`.","Чтобы понимать поведение кода, важно знать, где он “живёт”.\\nОпределение\\nОпределение: Глобальный контекст выполнения — это базовый контекст, в котором выполняется код вне функций, и который создаётся при загрузке страницы.\\nЧто входит в глобальный контекст браузера\\nВ браузере глобальным объектом является `window`:\\n\\n- глобальные переменные (`var a = 1`)\\n\\n- глобальные функции\\n\\n- Web API (`document`, `history`, `location`)\\n\\n- таймеры (`setTimeout`, `setInterval`)\\n\\nОсобенности `let` и `const`\\nПеременные, объявленные через `let` и `const`:\\n\\n- находятся в глобальной области видимости,\\n\\n- но не становятся свойствами `window`.\\n\\nЗачем это важно\\n\\n- понимание конфликтов имён\\n\\n- работа со сторонними скриптами\\n\\n- диагностика утечек глобального состояния\\n\\nВывод: в браузере глобальный контекст выполнения связан с объектом `window`, который хранит глобальные данные и предоставляет Web API.","var x = 10;\\nconsole.log(window.x); // 10\\n\\nlet y = 5;\\nconsole.log(window.y); // undefined"],[1277,"В браузере глобальным объектом является `window`, а в Node.js — `global`. В браузере код ориентирован на работу с DOM и Web API, а в Node.js — на файловую систему и серверные задачи. Кроме того, в Node.js каждый файл имеет собственный модульный контекст, а не общий глобальный scope, как в браузере. Это влияет на область видимости переменных и поведение `this`.","JavaScript один, но среда выполнения — разная.\\nОпределение\\nОпределение: Глобальный объект — объект верхнего уровня, через который доступно окружение выполнения JavaScript.\\nБраузер\\nВ браузере:\\n\\n- глобальный объект — `window`\\n\\n- доступен DOM (`document`)\\n\\n- глобальный `this` указывает на `window`\\n\\nNode.js\\nВ Node.js:\\n\\n- глобальный объект — `global`\\n\\n- нет DOM\\n\\n- каждый файл — отдельный модуль\\n\\nПеременные, объявленные в файле, не попадают напрямую в `global`.\\nПрактическое значение\\n\\n- код для браузера нельзя напрямую запускать в Node.js\\n\\n- библиотеки часто делают проверки окружения\\n\\n- важно для SSR и универсального JavaScript\\n\\nВывод: глобальный контекст зависит от среды: в браузере это `window`, в Node.js — `global`, и правила области видимости отличаются.","console.log(this === window); // true\\n\\nconsole.log(this === global); // false"],[1278,"Связный список можно реализовать с помощью объектов, где каждый элемент хранит значение и ссылку на следующий элемент. Начальный элемент называется `head`. Для добавления нового элемента нужно создать новый узел и изменить ссылку предыдущего. Такая структура удобна для частых вставок и удалений.","Связный список — это простая, но показательная структура данных.\\nОпределение\\nОпределение: Связный список — структура данных, состоящая из узлов, где каждый узел хранит значение и ссылку на следующий узел.\\nБазовая структура узла\\n\\nСоздание списка\\n\\nДобавление элемента\\n\\nКогда это полезно\\n\\n- частые вставки и удаления\\n\\n- обучение работе со ссылками\\n\\n- понимание принципов структур данных\\n\\nОграничения\\n\\n- медленный доступ по индексу\\n\\n- больше памяти, чем у массива\\n\\nВывод: связный список в JavaScript реализуется через объекты и ссылки между ними и полезен для понимания базовых алгоритмов и структур данных.","function Node(value) {\\n  this.value = value;\\n  this.next = null;\\n}\\n\\nconst head = new Node(1);\\nhead.next = new Node(2);\\nhead.next.next = new Node(3);\\n\\nfunction append(head, value) {\\n  let current = head;\\n\\n  while (current.next) {\\n    current = current.next;\\n  }\\n\\n  current.next = new Node(value);\\n}"],[1279,"Примитивы (например, `number`, `string`, `boolean`, `null`, `undefined`, `bigint`, `symbol`) передаются в функцию как значение. Это означает, что параметр функции получает копию значения, а не “ссылку” на исходную переменную. Если внутри функции изменить параметр, изменится только локальная копия. Снаружи исходная переменная останется прежней.","JavaScript передаёт аргументы по значению, но важно понимать, что “значение” у примитива и у объекта — разное. Для примитива это само значение (число, строка и т.д.), поэтому функция не может “дотянуться” до внешней переменной и изменить её напрямую.\\nОпределение\\nОпределение: Передача по значению (pass-by-value) — это когда параметр функции получает копию переданного значения, и дальнейшие изменения параметра не влияют на исходную переменную.\\nЧто происходит при вызове функции\\nКогда ты вызываешь функцию:\\n\\n- Вычисляется значение аргумента.\\n\\n- Создаётся параметр (локальная переменная функции).\\n\\n- В параметр записывается копия этого значения.\\n\\nПример:\\n\\n- `a` остаётся `10`, потому что `x` — отдельная локальная переменная.\\n\\n- `x = x + 1` — это переприсваивание локального параметра.\\n\\nПочему это важно в реальном коде\\n\\n- Предсказуемость\\n\\n- Функции, работающие с примитивами, по умолчанию “безопаснее”: они не меняют внешнее состояние, если явно не возвращают новое значение.\\n\\n- Иммутабельный стиль\\n\\n- В UI (в том числе в React) часто удобнее “не мутировать”, а возвращать новое значение.\\n\\nЧастая путаница: “передача по ссылке”\\nИногда говорят, что “примитив передаётся по значению, объект — по ссылке”. Точнее так:\\n\\n- JavaScript всегда передаёт по значению.\\n\\n- Для объектов это значение — ссылка (идентификатор) на объект.\\n\\nИз-за этого поведение объектов выглядит “как по ссылке”, но технически это “копия ссылки”.\\nВывод: примитивы передаются как копия значения, поэтому переприсваивание параметра внутри функции не влияет на переменную снаружи.","function inc(x) {\\n  x = x + 1;\\n  return x;\\n}\\n\\nlet a = 10;\\nconst b = inc(a);\\n\\nconsole.log(a); // 10\\nconsole.log(b); // 11"],[1280,"Потому что внутри функции ты меняешь не внешнюю переменную, а локальный параметр, который содержит копию значения. Примитив нельзя “изменить внутри” — можно только присвоить новое значение переменной. Когда ты делаешь `x = ...`, ты переприсваиваешь параметр `x`, а не исходную переменную. Поэтому снаружи ничего не меняется.","Тут важно различать два действия: изменить значение переменной и попытаться изменить “сам примитив”. Примитивы не имеют “внутреннего состояния”, которое можно мутировать — у них есть только значение, и переменная может указывать на одно значение или на другое.\\nОпределение\\nОпределение: Переприсваивание (reassignment) — это когда переменной дают новое значение, и она начинает хранить его вместо старого.\\nРазбор на примере\\n\\nЧто произошло:\\n\\n- В `setZero(n)` вычислилось значение `n` (это `5`).\\n\\n- Параметр `x` получил копию `5`.\\n\\n- `x = 0` изменил только локальную переменную `x`.\\n\\n- Переменная `n` вне функции не трогалась.\\n\\nПочему “внутри функции” нельзя поменять внешний примитив\\n\\n- У функции нет доступа к самой переменной `n`, она видит только значение, переданное при вызове.\\n\\n- Чтобы изменить внешнюю переменную, нужно:\\n\\n- вернуть новое значение и присвоить его снаружи, или\\n\\n- менять объект (если передан объект), или\\n\\n- использовать замыкание (если это переменная из внешнего scope, а не аргумент).\\n\\nПример с возвратом:\\n\\nПрактическая польза\\n\\n- такие функции легче тестировать\\n\\n- меньше “скрытых” побочных эффектов\\n\\n- проще поддерживать код в больших проектах\\n\\nВывод: изменение примитива “внутри функции” на самом деле является переприсваиванием локального параметра, поэтому внешняя переменная не меняется.","function setZero(x) {\\n  x = 0;\\n}\\n\\nlet n = 5;\\nsetZero(n);\\n\\nconsole.log(n); // 5\\n\\nfunction setZero(x) {\\n  return 0;\\n}\\n\\nlet n = 5;\\nn = setZero(n);\\n\\nconsole.log(n); // 0"],[1281,"Если передать в функцию объект, можно изменить его свойства внутри функции, и эти изменения будут видны снаружи. Это происходит потому, что параметр получает копию ссылки на один и тот же объект. Пока ты меняешь свойства (например, `obj.count++`), ты работаешь с тем же объектом. Но если ты переприсвоишь сам параметр (например, `obj = {}`), внешний объект не изменится.","Этот вопрос помогает понять важную модель: функция получает копию ссылки на объект, поэтому обе стороны “смотрят” на один и тот же объект в памяти.\\nОпределение\\nОпределение: Изменение объекта (mutation) — это изменение его внутреннего состояния (например, свойств), без создания нового объекта.\\nПример: изменение свойства внутри функции\\n\\nПочему это работает:\\n\\n- `state` хранит ссылку на объект `{ count: 0 }`.\\n\\n- В функцию передаётся значение этой ссылки (копия ссылки).\\n\\n- Параметр функции указывает на тот же объект.\\n\\n- `state.count += 1` меняет объект, поэтому результат виден снаружи.\\n\\nВажное отличие: изменение свойства vs переприсваивание параметра\\nИзменение свойства меняет тот же объект:\\n\\nПереприсваивание параметра не меняет внешний объект:\\n\\nКак это использовать “правильно”\\nТут есть два распространённых подхода — выбор зависит от стиля кода и требований.\\n\\n- Императивно (мутировать объект)\\n\\n- удобно в простом коде\\n\\n- быстро и без лишних аллокаций\\n\\n- но есть риск неожиданных побочных эффектов\\n\\n- Функционально (возвращать новый объект)\\n\\n- предсказуемее, особенно в UI\\n\\n- проще отслеживать изменения и делать сравнения\\n\\n- часто используется в React/Redux-подходах\\n\\nПример без мутаций:\\n\\nПрактическое замечание для React\\n\\n- В React состояние обычно обновляют без мутаций, чтобы сравнение ссылок помогало понять, что данные изменились.\\n\\n- Но локальные объекты (не state) можно мутировать, если это не влияет на рендер-логику.\\n\\nВывод: чтобы “изменить данные через функцию”, передавай объект и меняй его свойства — изменения будут видны снаружи, потому что функция получает копию ссылки на один и тот же объект. Если важна предсказуемость (например, в React), чаще возвращают новый объект вместо мутаций.","function incrementCounter(state) {\\n  state.count += 1;\\n}\\n\\nconst state = { count: 0 };\\nincrementCounter(state);\\n\\nconsole.log(state.count); // 1\\n\\nfunction setName(user) {\\n  user.name = \'Ann\';\\n}\\n\\nconst u = { name: \'Bob\' };\\nsetName(u);\\n\\nconsole.log(u.name); // \'Ann\'\\n\\nfunction replaceUser(user) {\\n  user = { name: \'Ann\' }; // новый объект, но ссылка только в локальном параметре\\n}\\n\\nconst u = { name: \'Bob\' };\\nreplaceUser(u);\\n\\nconsole.log(u.name); // \'Bob\'\\n\\nfunction incrementCounter(state) {\\n  return { ...state, count: state.count + 1 };\\n}\\n\\nlet state = { count: 0 };\\nstate = incrementCounter(state);"],[1282,"При использовании `var` в цикле все асинхронные колбэки будут ссылаться на одну и ту же переменную. К моменту выполнения асинхронного кода цикл уже завершится, и переменная будет иметь финальное значение. В результате вместо ожидаемых разных значений выводится одно и то же. Это происходит из-за функциональной, а не блочной области видимости `var`.","Проблема возникает из-за сочетания замыкания и области видимости `var`.\\nОпределение\\nОпределение: Замыкание (closure) — это ситуация, когда функция “запоминает” переменные из внешней области видимости и продолжает иметь к ним доступ позже.\\nЧто делает `var` в цикле\\n\\n- `var` имеет функциональную, а не блочную область видимости\\n\\n- в цикле создаётся одна переменная, а не новая на каждую итерацию\\n\\nПример проблемы\\n\\nЧто произойдёт:\\n\\n- Цикл выполнится полностью.\\n\\n- `i` станет равным `3`.\\n\\n- Асинхронные колбэки выполнятся позже.\\n\\n- Все они выведут `3`.\\n\\nПочему это неожиданно\\nРазработчик интуитивно ожидает:\\n\\nно получает:\\n\\nГде это чаще всего встречается\\n\\n- `setTimeout` / `setInterval`\\n\\n- обработчики событий в циклах\\n\\n- промисы, создаваемые внутри цикла\\n\\nВывод: при использовании `var` в асинхронных циклах все колбэки разделяют одну переменную, поэтому получают одно и то же итоговое значение.","for (var i = 0; i < 3; i++) {\\n  setTimeout(() => {\\n    console.log(i);\\n  }, 100);\\n}\\n\\n0\\n1\\n2\\n\\n3\\n3\\n3"],[1283,"Самый простой способ — использовать `let` вместо `var`, так как `let` создаёт новую переменную для каждой итерации цикла. Также можно использовать IIFE или передавать значение в функцию явно. Эти подходы позволяют каждому асинхронному колбэку работать со “своим” значением. В современном коде предпочтителен `let`.","Решение всегда сводится к одному: создать отдельную область видимости для каждой итерации.\\n1) Использование `let` (рекомендуемый способ)\\n\\nПочему работает:\\n\\n- `let` имеет блочную область видимости\\n\\n- на каждой итерации создаётся новая переменная `i`\\n\\n2) Использование IIFE (устаревающий, но показательный подход)\\n\\nЗдесь:\\n\\n- значение `i` копируется в параметр `value`\\n\\n- каждый колбэк замыкается на своё значение\\n\\n3) Передача значения в асинхронную функцию\\n\\nПрактическая рекомендация\\n\\n- Используй `let` по умолчанию\\n\\n- `var` оставляй только для legacy-кода\\n\\n- понимание IIFE важно для чтения старых проектов\\n\\nВывод: корректный вывод в асинхронном цикле достигается созданием отдельной области видимости для каждой итерации, чаще всего с помощью `let`.","for (let i = 0; i < 3; i++) {\\n  setTimeout(() => {\\n    console.log(i);\\n  }, 100);\\n}\\n\\nfor (var i = 0; i < 3; i++) {\\n  (function (value) {\\n    setTimeout(() => {\\n      console.log(value);\\n    }, 100);\\n  })(i);\\n}\\n\\nfunction logLater(value) {\\n  setTimeout(() => console.log(value), 100);\\n}\\n\\nfor (var i = 0; i < 3; i++) {\\n  logLater(i);\\n}"],[1284,"При сравнении объекта с числом оператор `==` пытается привести объект к примитиву. Для этого вызываются методы `valueOf` и `toString`. Если результат приведения не совпадает с числом, сравнение возвращает `false`. Поведение может выглядеть неожиданно, но оно строго определено спецификацией.","Когда в сравнении участвует объект, JavaScript сначала пытается превратить его в примитив.\\nОпределение\\nОпределение: ToPrimitive — алгоритм приведения объекта к примитивному значению перед операциями вроде `==`.\\nКак проходит сравнение\\nПри выражении:\\n\\nJavaScript делает следующее:\\n\\n- Видит, что один операнд — объект.\\n\\n- Запускает приведение объекта к примитиву.\\n\\n- Сравнивает получившийся примитив с числом.\\n\\nПример\\n\\nПочему:\\n\\n- `obj.valueOf()` → возвращает сам объект\\n\\n- `obj.toString()` → `\\"[object Object]\\"`\\n\\n- строка `\\"[object Object]\\"` не равна `1`\\n\\nПример, где сравнение будет true\\n\\nПочему это считается опасным\\n\\n- поведение неочевидно\\n\\n- легко получить баг в условиях\\n\\n- код сложно читать и поддерживать\\n\\nПрактическая рекомендация\\n\\n- избегать `==` при сравнении объектов\\n\\n- использовать `===`\\n\\n- явно приводить типы при необходимости\\n\\nВывод: объект не равен числу при `==`, потому что JavaScript сначала приводит объект к примитиву, и результат этого приведения чаще всего не совпадает с числом.","obj == 1\\n\\nconst obj = { value: 1 };\\n\\nobj == 1; // false\\n\\nconst obj = {\\n  valueOf() {\\n    return 1;\\n  }\\n};\\n\\nobj == 1; // true"],[1285,"При приведении объекта к примитиву JavaScript сначала пытается вызвать `valueOf`, а если он не возвращает примитив, вызывает `toString`. Этот порядок применяется по умолчанию для числового контекста. Если результат всё ещё не примитив, выбрасывается ошибка. Поведение строго описано в спецификации.","Когда объект участвует в операциях сравнения, сложения или преобразования типов, JavaScript запускает специальный алгоритм.\\nОпределение\\nОпределение: ToPrimitive — алгоритм преобразования объекта в примитивное значение перед выполнением операций.\\nБазовый порядок вызовов\\nПо умолчанию (числовой контекст):\\n\\n- Вызывается `obj.valueOf()`\\n\\n- Если результат — примитив, он используется\\n\\n- Иначе вызывается `obj.toString()`\\n\\n- Если результат — примитив, он используется\\n\\n- Иначе выбрасывается `TypeError`\\n\\nПример\\n\\n`valueOf` вернул объект, поэтому JavaScript перешёл к `toString`.\\nКонтекст имеет значение\\nДля строкового контекста порядок может меняться:\\n\\nВ этом случае JavaScript предпочтёт `toString`.\\nСовременный механизм\\nЕсли у объекта есть метод:\\n\\nон вызывается раньше `valueOf` и `toString` и полностью управляет приведением.\\nВывод: по умолчанию JavaScript сначала вызывает `valueOf`, затем `toString`, но порядок может зависеть от контекста и быть переопределён через `Symbol.toPrimitive`.","const obj = {\\n  valueOf() {\\n    return {};\\n  },\\n  toString() {\\n    return \'10\';\\n  }\\n};\\n\\nNumber(obj); // 10\\n\\nString(obj);\\n\\nobj[Symbol.toPrimitive]"],[1286,"Да, приведение типов у объекта можно переопределить. Для этого используют `Symbol.toPrimitive`, а также методы `valueOf` и `toString`. При наличии `Symbol.toPrimitive` именно он имеет наивысший приоритет. Это позволяет точно контролировать, как объект ведёт себя в разных контекстах.","JavaScript предоставляет несколько уровней контроля над приведением типов.\\nОпределение\\nОпределение: Symbol.toPrimitive — специальный метод, который определяет, как объект преобразуется в примитив.\\nПриоритет механизмов\\nJavaScript использует следующий порядок:\\n\\n- `obj[Symbol.toPrimitive]`\\n\\n- `valueOf()`\\n\\n- `toString()`\\n\\nПример с Symbol.toPrimitive\\n\\nКогда это может быть полезно\\n\\n- логирование и отладка\\n\\n- value-объекты (деньги, размеры, единицы измерения)\\n\\n- удобное API для библиотек\\n\\nПочему используют осторожно\\n\\n- ухудшает читаемость\\n\\n- может вводить в заблуждение\\n\\n- редко ожидаемо для других разработчиков\\n\\nВывод: да, приведение типов можно переопределить, и самый приоритетный способ — `Symbol.toPrimitive`, но применять его стоит осознанно.","const user = {\\n  name: \'Alex\',\\n  age: 30,\\n  [Symbol.toPrimitive](hint) {\\n    if (hint === \'number\') return this.age;\\n    if (hint === \'string\') return this.name;\\n    return `${this.name} (${this.age})`;\\n  }\\n};\\n\\nNumber(user); // 30\\nString(user); // \'Alex\'\\nuser + \'\'; // \'Alex (30)\'"],[1287,"`then` и `finally` выполняются после синхронного кода, потому что они ставятся в очередь микрозадач. JavaScript сначала выполняет весь синхронный код в текущем стеке вызовов. Только после этого event loop начинает обрабатывать микрозадачи. Поэтому обработчики промисов всегда выполняются “чуть позже”.","Чтобы понять это поведение, нужно разобрать модель выполнения JavaScript.\\nОпределение\\nОпределение: Event loop — механизм, который управляет выполнением синхронного кода, микрозадач и макрозадач.\\nКак выполняется код\\nJavaScript следует строгому порядку:\\n\\n- Выполняется весь синхронный код (call stack).\\n\\n- Затем выполняются все микрозадачи.\\n\\n- Потом берётся следующая макрозадача.\\n\\nГде находятся then и finally\\n\\n- `Promise.then`\\n\\n- `Promise.catch`\\n\\n- `Promise.finally`\\n\\nВсе они добавляются в очередь microtask queue.\\nПример\\n\\nПорядок вывода:\\n\\nПочему это важно\\n\\n- объясняет “неожиданный” порядок `console.log`\\n\\n- помогает отлаживать асинхронный код\\n\\n- критично для React, где много промисов и эффектов\\n\\nЧастая ошибка\\nОжидание, что `then` выполнится “сразу после resolve” внутри того же синхронного блока.\\nВывод: `then` и `finally` выполняются после синхронного кода, потому что они обрабатываются как микрозадачи и запускаются event loop только после очистки текущего стека вызовов.","console.log(\'start\');\\n\\nPromise.resolve().then(() => {\\n  console.log(\'then\');\\n});\\n\\nconsole.log(\'end\');\\n\\nstart\\nend\\nthen"],[1288,"Чтобы определить порядок вывода `console.log`, нужно понимать работу event loop. Сначала всегда выполняется синхронный код из call stack. Затем выполняются все микрозадачи (например, `then`, `catch`, `finally`). Только после этого обрабатываются макрозадачи (`setTimeout`, `setInterval`, события). Зная, к какой очереди относится операция, можно точно предсказать порядок логов.","Порядок выполнения в JavaScript не случайный — он строго регламентирован моделью event loop.\\nОпределение\\nОпределение: Event loop — механизм, который управляет очередями задач и определяет, какой код и когда будет выполнен.\\nОсновные участники\\nПеред анализом любого примера полезно помнить три уровня выполнения:\\n\\n- Call stack\\n\\n- весь синхронный код\\n\\n- Microtask queue\\n\\n- `Promise.then`\\n\\n- `Promise.catch`\\n\\n- `Promise.finally`\\n\\n- `queueMicrotask`\\n\\n- Macrotask queue\\n\\n- `setTimeout`\\n\\n- `setInterval`\\n\\n- события\\n\\n- `setImmediate` (в Node.js)\\n\\nАлгоритм определения порядка\\nЧтобы определить порядок `console.log`, действуй по шагам:\\n\\n- Выпиши весь синхронный код — он выполнится первым.\\n\\n- Найди микрозадачи и запомни порядок их постановки.\\n\\n- Найди макрозадачи.\\n\\n- После завершения синхронного кода:\\n\\n- сначала выполняются все микрозадачи,\\n\\n- потом одна макрозадача,\\n\\n- затем снова микрозадачи и так далее.\\n\\nПример с разбором\\n\\nРазбор:\\n\\n- Синхронно:\\n\\n- `A`\\n\\n- `D`\\n\\n- Микрозадачи:\\n\\n- `C`\\n\\n- Макрозадачи:\\n\\n- `B`\\n\\nИтоговый вывод:\\n\\nЧастые ловушки\\n\\n- `setTimeout(fn, 0)` не означает “выполнить сразу”\\n\\n- микрозадачи могут “задушить” макрозадачи, если их слишком много\\n\\n- `async/await` внутри себя тоже использует промисы и микрозадачи\\n\\nВывод: чтобы определить порядок вывода логов, нужно строго разделять синхронный код, микрозадачи и макрозадачи и применять правила event loop шаг за шагом.","console.log(\'A\');\\n\\nsetTimeout(() => {\\n  console.log(\'B\');\\n}, 0);\\n\\nPromise.resolve().then(() => {\\n  console.log(\'C\');\\n});\\n\\nconsole.log(\'D\');\\n\\nA\\nD\\nC\\nB"],[1289,"`NaN` — единственное значение в JavaScript, которое не равно самому себе. Поэтому проверка вида `value !== value` позволяет определить `NaN`. Такой способ работает без использования `Number.isNaN`. Однако он менее читаемый и используется редко в прикладном коде.","Хотя обычно используют `Number.isNaN`, полезно понимать, как можно определить `NaN` “вручную”.\\nОпределение\\nОпределение: NaN — единственное значение в JavaScript, для которого выражение `x !== x` возвращает `true`.\\nРеализация функции\\n\\nПочему это работает\\n\\n- любое нормальное значение равно самому себе\\n\\n- `NaN !== NaN` всегда `true`\\n\\n- других таких значений в языке нет\\n\\nПример использования\\n\\nОграничения подхода\\n\\n- работает только для `NaN`\\n\\n- менее очевиден для чтения\\n\\n- хуже подходит для production-кода\\n\\nВывод: проверка `value !== value` — валидный способ определить `NaN`, основанный на его уникальном свойстве, но обычно предпочтительнее `Number.isNaN`.","function isNaNValue(value) {\\n  return value !== value;\\n}\\n\\nisNaNValue(NaN); // true\\nisNaNValue(5);   // false\\nisNaNValue(\'a\'); // false"],[1290,"Потому что `NaN` по стандарту IEEE 754 не равно никакому значению, включая самого себя. Это поведение специально заложено в спецификацию. Все остальные значения в JavaScript равны самим себе. Поэтому сравнение значения с самим собой позволяет надёжно выявить `NaN`.","Поведение `NaN` кажется странным, но оно логично с точки зрения стандарта чисел с плавающей точкой.\\nОпределение\\nОпределение: IEEE 754 — стандарт представления чисел с плавающей точкой, используемый в JavaScript.\\nМатемическая идея\\n`NaN` означает “результат не определён”. Он не представляет конкретное число, поэтому:\\n\\n- `NaN == NaN` → `false`\\n\\n- `NaN === NaN` → `false`\\n\\nСвязь с проверкой\\nЛюбое значение `x` в JavaScript:\\n\\n- `x === x` → `true`\\n\\n- кроме `NaN`\\n\\nОтсюда и возникает проверка:\\n\\nПрактическая ценность\\n\\n- позволяет реализовать проверку без встроенных функций\\n\\n- используется в low-level коде\\n\\n- полезно для понимания языка, а не как основной приём\\n\\nВывод: сравнение аргумента с самим собой работает для определения `NaN`, потому что `NaN` — единственное значение, не равное самому себе.","value !== value"],[1291,"`Object.is` и `===` похожи, но ведут себя по-разному в двух случаях: при сравнении `NaN` и `+0` / `-0`. `Object.is(NaN, NaN)` возвращает `true`, а `NaN === NaN` — `false`. Также `Object.is(+0, -0)` возвращает `false`, тогда как `+0 === -0` — `true`. Во всех остальных случаях поведение одинаково.","`Object.is` был добавлен, чтобы дать более “точное” сравнение значений.\\nОпределение\\nОпределение: Object.is — метод сравнения, который проверяет значения по алгоритму SameValue.\\nКлючевые отличия от `===`\\nЕсть ровно два отличия.\\n1) NaN\\n\\n2) Знаковый ноль\\n\\nПочему это важно\\n\\n- для низкоуровневых сравнений\\n\\n- для корректной работы с числовыми алгоритмами\\n\\n- для библиотек и фреймворков\\n\\nСвязь с React\\nReact использует сравнение ссылок (`===`) для оптимизации, но в некоторых местах (например, `Object.is` в хуках) используется именно `Object.is` для корректной обработки `NaN`.\\nКогда что использовать\\n\\n- `===` — по умолчанию в прикладном коде\\n\\n- `Object.is` — когда важны крайние случаи\\n\\nВывод: `Object.is` отличается от `===` только в сравнении `NaN` и `+0 / -0`, но именно эти отличия делают его более точным инструментом сравнения.","Object.is(NaN, NaN); // true\\nNaN === NaN;         // false\\n\\nObject.is(+0, -0); // false\\n+0 === -0;         // true"],[1292,"Сначала выполняется весь синхронный код. Затем выполняются microtask, включая обработчики `Promise`. После этого выполняются macrotask, такие как `setTimeout`. Этот порядок повторяется на каждой итерации event loop. Поэтому `Promise` обычно выполняется раньше `setTimeout`.","JavaScript выполняется в одном потоке, но умеет обрабатывать асинхронные операции.\\nОпределение\\nEvent loop — это механизм, который управляет выполнением кода, очередями задач и стеком вызовов.\\nПорядок выполнения\\nПеред тем как перейти к асинхронному коду, JavaScript всегда выполняет синхронные инструкции.\\n\\n- Синхронный код\\n\\n- Выполняется сразу в call stack\\n\\n- Microtask queue\\n\\n- `Promise.then`\\n\\n- `queueMicrotask`\\n\\n- Macrotask queue\\n\\n- `setTimeout`\\n\\n- `setInterval`\\n\\n- события\\n\\nПример\\n\\nРезультат выполнения:\\n\\n- `A`\\n\\n- `D`\\n\\n- `C`\\n\\n- `B`\\n\\nВывод\\nПонимание очередей задач помогает предсказывать порядок выполнения кода и избегать ошибок в асинхронной логике.","console.log(\\"A\\");\\n\\nsetTimeout(() => console.log(\\"B\\"));\\n\\nPromise.resolve().then(() => console.log(\\"C\\"));\\n\\nconsole.log(\\"D\\");"],[1293,"Web APIs позволяют браузеру выполнять асинхронные операции вне основного потока JavaScript. Таймеры, сетевые запросы и события обрабатываются браузером, а не движком JavaScript. После завершения операции колбэк помещается в соответствующую очередь задач. Event loop затем решает, когда выполнить этот код.","JavaScript-движок сам по себе не умеет выполнять асинхронные операции.\\nЧто такое Web APIs\\nWeb APIs — это интерфейсы браузера, предоставляющие возможности, недоступные чистому JavaScript.\\nПримеры Web APIs\\n\\n- `setTimeout`\\n\\n- `fetch`\\n\\n- DOM Events\\n\\n- `WebSocket`\\n\\nКак они участвуют в event loop\\n\\n- Асинхронная операция передаётся в Web API\\n\\n- JavaScript продолжает выполнение синхронного кода\\n\\n- После завершения операции колбэк попадает в очередь задач\\n\\n- Event loop добавляет его в call stack\\n\\nПример\\n\\nТаймер обрабатывается Web API, а функция будет выполнена позже через event loop.\\nВывод\\nWeb APIs расширяют возможности JavaScript и позволяют реализовывать асинхронное поведение без блокировки основного потока.","setTimeout(() => {\\n  console.log(\\"Готово\\");\\n}, 1000);"],[1294,"`MutationObserver` позволяет отслеживать изменения в DOM без постоянных проверок. Он реагирует на добавление, удаление или изменение узлов. `queueMicrotask` позволяет вручную добавить задачу в очередь microtask. Оба механизма тесно связаны с event loop и выполняются до macrotask.","Работа с DOM и асинхронностью часто требует точного контроля момента выполнения кода.\\nMutationObserver\\nMutationObserver — это API для наблюдения за изменениями в DOM-дереве.\\nЧто он умеет отслеживать\\n\\n- Добавление и удаление узлов\\n\\n- Изменение атрибутов\\n\\n- Изменение текстового содержимого\\n\\nПример\\n\\nКолбэк будет вызван асинхронно после изменений DOM.\\nqueueMicrotask\\nqueueMicrotask — это функция для добавления задачи в очередь microtask.\\nОсобенности\\n\\n- Выполняется после текущего синхронного кода\\n\\n- Выполняется до `setTimeout`\\n\\n- Аналогична `Promise.then`, но без создания Promise\\n\\nПример\\n\\nСвязь с event loop\\n\\n- DOM-изменения фиксируются\\n\\n- MutationObserver добавляет задачу в microtask queue\\n\\n- Event loop выполняет microtask до macrotask\\n\\nВывод\\n`MutationObserver` подходит для реакций на изменения DOM, а `queueMicrotask` — для точного управления порядком выполнения асинхронного кода.","const observer = new MutationObserver((mutations) => {\\n  console.log(mutations);\\n});\\n\\nobserver.observe(document.body, {\\n  childList: true,\\n  subtree: true,\\n});\\n\\nqueueMicrotask(() => {\\n  console.log(\\"microtask\\");\\n});"],[1295,"Глубокое копирование создаёт новую копию объекта со всеми вложенными структурами. Один из простых способов — сериализация через JSON, но он имеет ограничения. Современный и безопасный способ — `structuredClone`. Также можно реализовать рекурсивное копирование вручную. Выбор метода зависит от типов данных.","Поверхностное копирование копирует только ссылки, поэтому изменения затрагивают исходный объект.\\nОпределение\\nГлубокое копирование — это создание нового объекта со всеми вложенными значениями без общих ссылок.\\nОсновные способы\\n\\n- JSON-сериализация\\n\\nПодходит только для простых данных.\\n\\n- structuredClone\\n\\nПоддерживает большинство встроенных типов.\\n\\n- Рекурсивное копирование\\n\\n- Используется для полного контроля логики\\n\\n- Требует обработки циклических ссылок\\n\\nОграничения методов\\n\\n- JSON не копирует функции и `undefined`\\n\\n- structuredClone недоступен в очень старых браузерах\\n\\nВывод\\nДля современных приложений предпочтительно использовать `structuredClone`, а JSON — только для простых структур данных.","const copy = JSON.parse(JSON.stringify(obj));\\n\\nconst copy = structuredClone(obj);"],[1296,"JSON поддерживает ограниченный набор типов данных. При сериализации функции, `undefined` и символы теряются. Некоторые типы преобразуются в строки или игнорируются. Это связано с тем, что JSON предназначен для передачи данных, а не логики.","JSON — это текстовый формат обмена данными с жёсткими правилами.\\nКакие типы поддерживает JSON\\n\\n- `string`\\n\\n- `number`\\n\\n- `boolean`\\n\\n- `null`\\n\\n- `object`\\n\\n- `array`\\n\\nКакие данные теряются\\n\\n- Функции\\n\\n- `undefined`\\n\\n- `Symbol`\\n\\n- Циклические ссылки\\n\\nПример\\n\\nПочему это происходит\\nJSON был создан как формат передачи данных между системами, поэтому поддерживает только универсальные примитивы.\\nВывод\\nJSON подходит для хранения и передачи данных, но не для сложных структур JavaScript-объектов.","const obj = {\\n  a: 1,\\n  b: undefined,\\n  c: () => {},\\n};\\n\\nJSON.stringify(obj); // {\\"a\\":1}"],[1297,"Throttle — это приём, который ограничивает частоту вызова функции. Функция выполняется не чаще заданного интервала времени. Все вызовы между интервалами игнорируются или объединяются. Это часто используется для оптимизации производительности.","При работе с событиями браузера функция может вызываться слишком часто.\\nОпределение\\nThrottle — это техника, ограничивающая выполнение функции одним вызовом за определённый промежуток времени.\\nГде применяется throttle\\n\\n- Обработка `scroll`\\n\\n- Обработка `resize`\\n\\n- Отслеживание движения мыши\\n\\nПример\\n\\nОтличие от debounce\\n\\n- Throttle — выполняет регулярно\\n\\n- Debounce — выполняет после паузы\\n\\nВывод\\nThrottle полезен, когда важно регулярно обрабатывать события, но без перегрузки приложения.","function throttle(fn, delay) {\\n  let isThrottled = false;\\n\\n  return function () {\\n    if (isThrottled) return;\\n\\n    isThrottled = true;\\n    fn();\\n\\n    setTimeout(() => {\\n      isThrottled = false;\\n    }, delay);\\n  };\\n}"],[1298,"Для оптимизации поиска по большим массивам данных можно использовать несколько подходов:\\n\\n- Делать поиск асинхронным, используя Web Workers или setTimeout.\\n\\n- Разбивать данные на страницы (пагинация) и загружать только нужную часть.\\n\\n- Использовать алгоритмы поиска с меньшей сложностью, такие как бинарный поиск для отсортированных данных.","Когда данные слишком большие, и мы пытаемся обработать их все сразу на клиенте, это может привести к значительной нагрузке на браузер, особенно на мобильных устройствах. Вот несколько способов решить эту проблему:\\n\\n- Асинхронный поиск:\\n\\n- Применение `Web Workers` позволяет выполнять поиск в фоновом потоке, не блокируя основной поток UI.\\n\\n- Для меньших объемов можно использовать `setTimeout`, чтобы делить поиск на части и обновлять UI постепенно.\\n\\n- Пагинация данных:\\n\\n- Вместо того, чтобы загружать все данные сразу, можно разбить их на страницы. Пользователь будет работать только с небольшой частью данных.\\n\\n- Это не только ускоряет поиск, но и значительно снижает нагрузку на браузер.\\n\\n- Использование эффективных алгоритмов поиска:\\n\\n- Для отсортированных данных можно использовать бинарный поиск, который имеет сложность O(log n), в отличие от линейного поиска O(n).\\n\\nВывод:\\nДля больших массивов данных важно оптимизировать как поиск, так и загрузку данных. Пагинация, асинхронные подходы и эффективные алгоритмы могут значительно повысить производительность.",null],[1299,"HTTP/2 улучшает производительность запросов благодаря следующим особенностям:\\n\\n- Мультиплексирование — позволяет отправлять несколько запросов и получать ответы по одному соединению.\\n\\n- Сжатие заголовков — уменьшает объем передаваемых данных.\\n\\n- Серверная отправка (server push) — позволяет серверу отправлять ресурсы заранее, даже если клиент их еще не запросил.","HTTP/2 представляет собой улучшенную версию протокола HTTP/1.1, с несколькими основными улучшениями:\\n\\n- Мультиплексирование:\\n\\n- В HTTP/1.1 каждый запрос создавал отдельное соединение, что привело к излишним затратам на установку соединений и их закрытие. HTTP/2 решает это с помощью мультиплексирования, позволяя отправлять несколько запросов и получать несколько ответов по одному TCP-соединению.\\n\\n- Сжатие заголовков:\\n\\n- В HTTP/1.1 заголовки каждого запроса передавались как текст, что добавляло много лишних данных. HTTP/2 использует алгоритм сжатия HPACK, который значительно уменьшает размер заголовков, тем самым ускоряя передачу данных.\\n\\n- Серверная отправка (server push):\\n\\n- Сервер может отправить ресурсы (например, изображения или стили), которые, по его мнению, будут необходимы клиенту, даже если клиент не запрашивал их еще. Это позволяет уменьшить задержку, поскольку ресурсы загружаются заранее.\\n\\nВывод:\\nHTTP/2 значительно повышает производительность веб-приложений благодаря оптимизации передачи данных и улучшению работы с соединениями.",null],[1300,"HTTP (Hypertext Transfer Protocol) — это протокол передачи данных в сети. Он используется для обмена информацией между клиентом и сервером. HTTP поддерживает различные методы запросов:\\n\\n- GET — получение данных.\\n\\n- POST — отправка данных на сервер.\\n\\n- PUT — обновление данных.\\n\\n- DELETE — удаление данных.","HTTP — это основной протокол для обмена данными в интернете. Он используется в основном для загрузки веб-страниц, но также для взаимодействия с API и другими сервисами. Вот ключевые методы, которые поддерживает HTTP:\\n\\n- GET — используется для получения данных с сервера. Например, когда вы заходите на веб-страницу, ваш браузер отправляет GET-запрос к серверу для получения HTML, CSS и других ресурсов.\\n\\n- POST — используется для отправки данных на сервер. Например, отправка формы с данными пользователя (имя, пароль и т. д.) на сервер для обработки.\\n\\n- PUT — используется для обновления существующих данных на сервере. Например, при редактировании записи в базе данных.\\n\\n- DELETE — используется для удаления данных с сервера. Это может быть удаление записи в базе данных или файла.\\n\\nВывод:\\nHTTP — это фундамент для работы веб-приложений, а методы GET, POST, PUT и DELETE являются основными инструментами для взаимодействия с сервером.",null]]');export{e as default};